import { expect, it } from 'vitest';
import { resolveTrainingCalendar } from './calendar';
const p = { timeZone: 'America/New_York', weeklyTarget: 5 as const, pending: null };
it('uses Monday local dates and actual DST boundaries', () => { expect(resolveTrainingCalendar('2026-09-14T03:59:59Z', p)).toMatchObject({ localDate: '2026-09-13', weekStartLocalDate: '2026-09-07', nextWeekAtUtc: '2026-09-14T04:00:00.000Z' }); expect(resolveTrainingCalendar('2026-09-14T04:00:00Z', p).weekStartLocalDate).toBe('2026-09-14'); expect(resolveTrainingCalendar('2026-03-08T12:00:00Z', p).nextWeekAtUtc).toBe('2026-03-09T04:00:00.000Z'); expect(resolveTrainingCalendar('2026-11-01T12:00:00Z', p).nextWeekAtUtc).toBe('2026-11-02T05:00:00.000Z'); });
it('rejects invalid zones', () => expect(() => resolveTrainingCalendar('2026-09-11T12:00:00Z', { ...p, timeZone: 'invented' })).toThrow());
it('keeps pending timezone changes behind the original-zone Monday instant', () => { const pending = { ...p, pending: { timeZone: 'Pacific/Honolulu', weeklyTarget: 3 as const, effectiveAtUtc: '2026-09-14T04:00:00.000Z' } }; expect(resolveTrainingCalendar('2026-09-14T03:59:59.000Z', pending)).toMatchObject({ timeZone: 'America/New_York', localDate: '2026-09-13' }); expect(resolveTrainingCalendar('2026-09-14T04:00:00.000Z', pending)).toMatchObject({ timeZone: 'Pacific/Honolulu', localDate: '2026-09-13' }); });
