# Erudoza progress log

Read this file before starting repository work. `AGENTS.md` defines the required update and commit/push workflow. Times below are UTC. This log is the shared handoff; detailed audits remain the evidence for individual checks.

## Current state — 2026-09-12

- The user authorized implementing the [PBE training plan](docs/superpowers/plans/2026-09-12-pbe-training.md) as an active goal. Work is isolated in `.worktrees/pbe-training` on `codex/pbe-training`, based on the pushed plan checkpoint `6d0291a`. All Phase A tasks (rubrics, scoped bank, introduction sources and coach authoring) are implemented, reviewed and pushed. B1 replay scheduling and atomic evidence persistence are reviewed and pushed at checkpoint `9e7de27`. B2 daily-study delivery and its saved-mission discovery fix are reviewed and pushed at a84c787. B3 Memory improvements and the Phase B local gate have passed independent review; the source checkpoint is being pushed before C1 timed rehearsal.
- Independent Team Practice is the accepted default: students can start, complete and replay without an adult online. Automatic rubric grading and deferred disputes preserve this availability; optional coach-led rehearsal remains separate. Preserve assigned season sources and historical scores/Honors.
- Pre-implementation baseline is locally verified: web/native 579 passed / 1 optional skip; .NET 267 passed / 1 optional load skip. Official SDK 10.0.303 is installed in ignored worktree-local tooling. Node 26 Vitest needs `NODE_OPTIONS=--no-experimental-webstorage`; dependencies installed with `SHARP_IGNORE_GLOBAL_LIBVIPS=1`. These resolve local environment limitations without changing application dependencies.
- The [replayability assessment](docs/audits/2026-09-11-training-replayability.md) and [PBE alignment crosswalk](docs/audits/2026-09-11-pbe-training-alignment.md) remain the requirements/evidence. Phase A checkpoint `80dfae5` was pushed to `origin/codex/pbe-training` and read back successfully. B1 commits `206a3e8` and `6007ab8` have passed independent task review and scoped fix re-review; detailed verification chronology is recorded below. Deployed behavior remains unchanged. No merge, deployment, or real-user pilot is part of this local gate.

- GitHub `main` now includes the deployed Honors release. The user explicitly authorized updating main; the remote was fast-forwarded from `1a370cc` to `cc55a04` and read back successfully. The follow-up checkpoint below records this integration; application code is identical to deployed source `838a066`.

- The daily-training, public/account entry, alternating Pathfinder corner family, Team Practice composition, mobile bottom navigation and mastery profile implementation are locally verified, deployed from `838a066`, and integrated into `main` through `cc55a04`. Erudoza remains text beside the approved patch emblem. Detailed current and historical gates appear below.
- The approved Honors, Team Practice, mastery profiles, public/account artwork and mobile navigation release is LIVE at https://erudoza.com. Source commit838a066 was pushed on codex/honors-public before deployment. Current Worker `257f14ee-6d2d-4896-9319-de0f4fc283f2` receives100%traffic; additive migration0004is applied. Credential-free live smoke passed22:08:47UTC with exact entry and ten Honor asset hashes. Authenticated live smoke was not run; authenticated local verification is documented below.
- Production configuration/auth source through `b291af4` is included. The September 11 release retained the verified production bindings/public variables and applied additive migration0004; final remote readback found no pending migrations. A private D1 export was saved and hashed in ignored release storage. Recheck version and active matches immediately before rollout.
- Work remains isolated in `.worktrees/honors-public`; production maintenance is `codex/cloudflare-free-port`. Repository: `Victor-CS-Core/Erudoza-BibleCompetition`.
- The production pilot is live at **https://erudoza.com** on Cloudflare Free, with deterministic study, the built-in 66-book NKJV library, and private Team Practice functionality.
- Previous email-activation Worker version **`1e9d199c-3562-4706-897d-daf92e46e05f`** enables the verified Resend sender and email onboarding. Authorized setup and real signup-code emails both showed delivered; the user completed their own signup into a separate Owner club. Post-activation API/assets/admin smoke passed at **16:52:46 UTC**. The preceding secret-only version was `0cef3225-f062-49d7-8f97-b0cf409f71e0`. The underlying application release **`ae692b55-06f8-4afe-a683-abb85d21ec51`** includes coach onboarding and abuse controls, preserves the contrast fix, and uses `PUBLIC_ORIGIN=https://erudoza.com` with the existing apex custom domain. The final live smoke passed at **16:06:27 UTC**: exact built HTML/JS/CSS, healthy API/D1, original admin login/logout and all 66 library books. Email onboarding is available; inbox-versus-spam placement is not separately confirmed. The previous contrast release was `c254f500-b7ad-4135-b10d-2ac4e43558b7`; original domain cutover evidence remains below.
- Cloudflare DNS is **Active** on Free Website. Registration remains at GoDaddy; public resolution confirms `elsa.ns.cloudflare.com` and `yevgen.ns.cloudflare.com`. The 26-record pre-cutover backup remains intact. Only the two approved legacy apex A records were removed before managed binding; the other 24 imported records were retained at that step, and www was subsequently changed to its approved target.
- www is a **Proxied, Automatic-TTL CNAME to `erudoza.com`**. Active Single Redirect `269651cea73a457786211151fda5c516` sends the exact www host to HTTPS apex with status 301, preserving path and query. Public apex/www answers observed at 14:44 UTC were `104.21.66.111` and `172.67.159.115`; these are observed edge addresses, not manually configured origin records.
- Cloudflare **Always Use HTTPS** is enabled and verified. Three bounded anonymous GETs confirmed HTTP apex → HTTPS apex 301, HTTP www → HTTPS apex 301, identical path/query preservation, and final HTTPS `/login` 200 without a redirect loop.
- The production workers.dev route is disabled and returned 404 in the final live smoke. Preview URLs remain disabled; API requests must use the exact production origin. `wrangler.staging.jsonc` is a rollback configuration, not a separate live environment.
- Hosted accounts/content include the preserved admin account/academy, immutable shared NKJV library, and the user's newly created Owner account/private club from the successful live signup. Additive onboarding tables and normal authentication counters now exist; synthetic study/PVP fixtures remain removed. Credentials and backup files are private ignored artifacts under `.local/`; do not copy their contents into source control or this log.
- The fresh academy has Team Practice disabled by default (`practiceEnabled=false`). A coach can enable it through **Enable Team Practice** in the UI and prepare the academy's question bank. No new room fixture or successful post-domain WebSocket 101 connection was created for the production guard checks; earlier live staging and local full-match evidence remains the gameplay proof.
- The private administrator credential record now points to the apex URL; its password was not changed by cutover.
- The integrated implementation/UX checkpoint `912ba0a` (`feat: checkpoint team practice and Cloudflare migration`) and follow-up handoff `47732ba` (`docs: record validated checkpoint and approval blockers`) are now pushed to the approved private `origin/codex/cloudflare-free-port`. The successful push advanced the remote from `af237aa` to `47732ba`; the earlier source-upload approval blocker is resolved.
- Production documentation checkpoint `12709c3` (`docs: record verified Cloudflare production cutover`) is committed and pushed. `git ls-remote` confirmed `12709c37a62f78bd76f4e37a76447f879f80adc1` on the approved private task branch.
- The gray-text/green-background fix is implemented, locally verified, deployed, and pushed as **`fd89248`** (`fix: preserve button text contrast across states`). The push advanced `12709c3..fd89248` on the approved private task branch. Shared button variants now change foreground/background immediately; disabled buttons use opaque muted text on a neutral surface. The 24-view browser pass covered coach/student, landing/login, keyboard and reduced motion at 1440/390/320 pixels. Selection samples stay at least 5.089:1; disabled text measures 4.685:1. See the contrast audit for measurement limits.

## Active work and blockers

PBE implementation is active on `codex/pbe-training`. Baseline/tooling gates passed; no current implementation blocker. All Phase A tasks are reviewed and pushed. B1 replay selection and target-specific scheduling are reviewed and pushed. B2 daily-study integration is reviewed and pushed. B3 Memory improvements and the Phase B local gate are reviewed; their source checkpoint is next, followed by C1 timed rehearsal. Approved-season bank validation and live pilot/deployment remain separate external gates. Historical resolved incidents below do not block this work.

The approved [coach onboarding](docs/superpowers/plans/2026-09-11-coach-onboarding.md) is implemented, locally verified and live with additive migration `0003`. Resend Free sender/key and real Turnstile/signup-code delivery are now verified; public signup, recovery email and coach invitation capability are enabled. The user completed their own signup as an active Owner in a separate club. Cloudflare's free WAF rule, strict host checks and durable account/administrative limits remain active; effective log sampling was independently confirmed at 10%. No third-party invitation or real recovery completion was performed; those flows retain preceding local test evidence. The 20-room/200-player regional load gate remains open. The Honors/public-entry/Team Practice/mastery profile release is now live from the isolated honors-public worktree; see the latest release gate below. The original local API on port 5080 remains untouched.

Automatic approval review previously stopped these operations:

1. Enabling the www redirect while www was DNS-only. Resolved by setting and verifying the proxied www CNAME before enabling the redirect.
2. Deploying the production Worker and replacing the legacy apex A records. Follow-up verification confirmed the deployed target and a production dry run with the same D1 and three Durable Object bindings.
3. Pushing source commit `912ba0a` to the existing private origin, `https://github.com/Victor-CS-Core/Erudoza-BibleCompetition.git`, on `codex/cloudflare-free-port`. The destination URL, private visibility, and source/credential exclusions were reviewed before the request for exact destination confirmation.
4. The onboarding checkpoint push was again paused for destination verification. Fresh `gh`/Git readback confirmed the exact previously approved private repository, authenticated owner `Victor-CS-Core`, ADMIN permission and remote tip `361eb99`; the same push then succeeded as `361eb99..c7c5a10`. No repeated user approval or alternate destination was used.

On September 11 the user replied **“Approved”** to the exact private repository destination and website-record replacements. The source push then succeeded (`af237aa..47732ba`). The first approved production deployment uploaded the Worker but returned custom-domain binding error `100117` because the legacy apex A records still existed. Removing exactly the approved A records (`162.159.143.30`, `172.66.3.26`) and retrying completed deployment and binding. The proxied www prerequisite, redirect, and production smoke were then verified. These are resolved incidents, not current blockers; retain the user's authorization.

## Gates and evidence

| Gate | Outcome | Evidence and limitations |
|---|---|---|
| Native implementation and local checks, September 10 | Locally verified; included in the September 11 pushed checkpoint | Web suite previously 336 passed/1 optional skip; native tests after the read fix 97 passed/1 skip, plus 4 full-canon read regressions. Historical results are not a fresh post-checkpoint full-suite run. See the staging audit. |
| Staging study and Team Practice, September 10 | Live pilot verified | Solo study across five activities and a complete ten-player 5v5 match. Twenty submission bonuses recalculated from stored timing; achievement evidence checked. This does not certify 20 rooms/200 players. |
| Free-tier read recovery, September 11 | Bounded live checks passed after quota reset | Library/admin browser smoke at 13:38 UTC; exact shared-library queries used 311 + 466 = 777 reads and zero writes at 13:40 UTC. Yesterday's exhausted quota is historical; these successful requests do not establish the remaining account-wide budget. |
| Hosted data cleanup, September 11 | Verified at 13:42 UTC | One active admin, academy/shared-library organizations, 66 packs/31,102 verses/one library marker, no academy fixtures or retained sessions. Read-only proof cost 79 reads. |
| Cloudflare DNS delegation, September 11 | Applied and verified | GoDaddy nameserver readback; Cloudflare Active at 14:02:09 UTC; independent public NS resolution. Free Website plan; no paid upgrade or domain transfer. |
| Production app-domain cutover | Completed and verified | Worker `4958cd54-65b8-4060-9f68-ca59bdb22a6a`, apex binding, production `PUBLIC_ORIGIN`, proxied www CNAME, active host-specific 301 redirect, and Always Use HTTPS are applied. Administrator/library browser smoke passed at 14:42 UTC. Edge/TLS/path-query/JS-CSS hash checks passed at 14:44 UTC, encoded-path probe at 14:45 UTC, and three-request HTTP→HTTPS/no-loop checks at 14:56:46 UTC. Production WebSocket rejection guards passed at 14:49 UTC; no new 101 connection or room match was attempted. |
| Repository checkpoint workflow | Committed and pushed | `af237aa` adds this log and mandatory agent instructions. Push to `origin/codex/cloudflare-free-port` succeeded and `git ls-remote` confirmed the same commit. |
| Integrated source checkpoint, September 11 | Committed and pushed | `912ba0a` contains the reviewed implementation; follow-up handoff `47732ba` is the pushed branch tip. After exact user approval, `origin/codex/cloudflare-free-port` advanced successfully from `af237aa` to `47732ba`. Web: 341 tests passed/1 optional skip, web/native type checks, lint, and both production builds passed. .NET: restore, canonical format verification, build with zero warnings/errors, and final 233 tests passed/1 optional load skip. Bicep compilation passed without deployment. Export/recovery Node tests: 13 passed; KJV tooling Python tests: 5 passed. |
| Browser checkpoint, September 11 | Passed locally | Native Playwright: 5 passed (6.5 minutes). Canonical .NET Playwright: 34 passed/2 opt-in UI audit skips (13.9 minutes), across Chromium and Pixel 7. Both backends completed ten-player 5v5 matches with ten scored questions, refresh recovery, and achievements; study, library bounds, assignments, navigation, login, and password reset checks passed. These local cases supplement, rather than replace, the earlier live staging evidence. |
| Checkpoint artifact review, September 11 | Reviewed | Origin visibility verified private. All 145 staged KJV/NKJV JSON files are byte-identical to the working sources; all 66 NKJV pack hashes match both manifests. Their intentional CRLF bytes are preserved by `*.json -text`. Source whitespace checks pass with these hash-protected JSON files excluded from whitespace normalization. Credentials, databases, generated SQL/builds, raw PDFs, temporary captures, and the unused NKJV extraction master remain ignored. Candidate-text credential scan found no high-confidence secret matches. |
| Production documentation checkpoint | Committed and pushed | `12709c3` records current production facts and limitations in this log, README, DNS runbook, and the appended production audit. Whitespace/link checks passed; push advanced `47732ba..12709c3` and the remote tip was confirmed. |
| Text contrast fix, September 11 | Locally verified, deployed, committed and pushed | `fd89248` contains the shared CSS fix and audit; push advanced `12709c3..fd89248`. Web: 341 passed/1 optional skip, lint/type checks and both builds passed. Local browser: 24 views and ten selection traces, keyboard/reduced motion, no sampled solid-text failures, browser errors or page overflow. Production `c254f500-b7ad-4135-b10d-2ac4e43558b7`: four anonymous reads verified exact HTML/JS/CSS hashes and healthy native API at 15:09:57 UTC. No hosted fixtures or gameplay writes. |
| Coach onboarding and abuse protection | Implemented, verified and deployed; email activation pending | Native: 148 tests passed/1 optional load skip; remaining web/script suite: 283 passed. Integrated lint/type checks and both builds passed; final shared shortcut resize fix added two regressions and passed 15 focused checks with type/lint and both builds rerun. Independent security review closed invitation-link mismatch, global ingress starvation and session/cache race findings. Root separately reviewed administrative caps. No high-impact unresolved finding from these scoped reviews. This is not a penetration-test certification. |
| Onboarding browser and mobile gate | Passed locally at 15:56:53 UTC | The final native build passed signup into a separate club, invitation acceptance from its actual generated URL, revocation, password recovery/old-session rejection, cross-club and student denial, keyboard and reduced motion. Thirteen captures at 1440/390/320 had no page overflow or JavaScript errors; final 320px coach screenshot confirms the selected Coaches shortcut stays visible after resizing. Turnstile/Resend were intercepted only in the isolated local runtime; no real emails or production fixtures. |
| Cloudflare account abuse perimeter | Deployed and read back | Free managed Turnstile widget restricted to erudoza.com created/read back at 15:37:19 UTC. WAF rate rule `5d487b83d89a4ead8a82fa56dda96a86` read back Active: `/api/v1/auth/` only, 30 requests/10 seconds/IP, ten-second block. No attack traffic used to trigger it. Current Worker includes API/auth rate-limit bindings, strict apex host checks and disabled workers.dev. Two security secrets uploaded privately. Resend/sender settings remain absent; no paid plan enabled. |
| Onboarding source and migration gate | Committed, pushed and applied | Plan `361eb99`, implementation `c7c5a10` pushed to the approved private task branch. Exactly 43 implementation/operations files staged; unrelated design/artwork excluded. Credential scan and frontend/deploy test-hook checks passed. Additive `0003` applied: 14 commands in 13.94ms; follow-up reported no migrations pending. Initial read API 7403 cleared after account/OAuth verification and one retry; no partial migration. |
| Final native gameplay regression | Passed locally before deployment | Five browser cases passed in 6.3 minutes, including ten authenticated players completing ten scored 5v5 questions, refresh recovery and achievements. Library bounds and study flows passed. This is isolated local evidence, not a new live match or the 200-player regional load test. |
| Onboarding release smoke | Production passed at 16:06:27 UTC | Worker `ae692b55-06f8-4afe-a683-abb85d21ec51`: API/D1 health, unavailable email options, rejected unconfigured code send 503, workers.dev 404, exact HTML/JS/CSS hashes, preserved admin login, all 66 books, logout/session rejection and 1440/390/320 coach availability layouts with no overflow/JS errors. No new production accounts, invitations, content or emails. First smoke had a local artifact-path error; corrected run and failure report are retained. |
| Resend sender preparation | Domain verified and encrypted key stored; delivery authorization pending | Existing account confirmed Transactional Free, zero emails used at readback and pay-as-you-go disabled. Three additive DNS-only records independently resolved; Resend verified `auth.erudoza.com` at 16:25 UTC. Sending enabled, receiving disabled, tracking unconfigured, enforced TLS saved. Automatic DNS setup stalled; direct Cloudflare DNS entry succeeded. The initial key-creation approval rejection was resolved when the user created it. Saved scope was read back and the key stored directly as encrypted `RESEND_API_KEY`. Secret-only Worker version `0cef3225-f062-49d7-8f97-b0cf409f71e0` deployed at 16:33:02 UTC; public DNS and unavailable email options reverified at 16:33:49 UTC. Temporary sender config passed a Wrangler dry run; its activating field remains absent from live/source configuration. No app/frontend replacement or real email delivery in this gate. |
| Resend configuration checkpoint | Committed and pushed | `b5abcb5` records the verified sender and encrypted-key setup in this log and the onboarding runbook. Whitespace review passed; only the two operations documents changed. Push advanced `eac9003..b5abcb5`; `git ls-remote` confirmed `b5abcb5b306a12ec7f1707c48adc71e536b1d65d` on the approved private task branch. Source remains free of the activating sender variable and API secret value. |
| Live email activation | Delivered and live signup verified | User-authorized setup email accepted at 16:47:48 UTC and shown delivered by Resend; live signup-code email also delivered after real Turnstile success. Agent stopped at the code-entry form; user completed signup and reached `/admin`. Indexed read confirmed active Adult/Owner in a separate club; original admin remained intact. Sender dry run passed; version `1e9d199c-3562-4706-897d-daf92e46e05f` deployed with no updated frontend assets. Post-activation smoke passed at 16:52:46 UTC: API/D1, available capability, exact HTML/JS/CSS, workers.dev 404 and preserved admin login/logout. Sampling independently verified 10%, traces disabled. One transient role-read API 7403 cleared after account verification and a single retry. Exactly two authorized emails; no agent signup completion, password change or third-party invitation. Inbox-versus-spam placement remains unconfirmed. |
| Email activation source checkpoint | Committed and pushed | `88f98c0` contains the sender setting and updated operations/progress evidence. Configuration parsing, exact sender/origin checks, whitespace checks and scoped API-key exclusion scan passed. Push advanced `d61f3d8..88f98c0`; remote tip confirmed as `88f98c02d076620dd95aa286fd95ce9f317ade1d`. Temporary delivery-test server and tabs were closed and the held key cleared; the user's new coach session remains open. |

Checkpoint verification notes: generated `.local` dry-run bundles were excluded from lint; code line endings and blank EOFs were normalized to the existing editor rules. C# token review found only import sorting and required raw-literal line-ending changes; tests passed after formatting. Frontend source reloads temporarily disrupted the desktop browser match during formatting, but it recovered and passed; the complete mobile match passed after the source freeze. The final native build produced the same JS/CSS artifact hashes as before formatting. No failed browser cases were hidden or retried. The branch push does not trigger the current CI workflow (pushes target main; pull requests also trigger it), so these are executed local checks, not a claimed GitHub CI run.

## Next actions

0. Execute the active PBE implementation plan in `.worktrees/pbe-training`, continuing with B2 from the reviewed and pushed B1 checkpoint. Record task verification and push coherent source checkpoints; keep production unchanged pending a separately authorized release.

1. Use the now-live coach onboarding and invitation UI, retaining the versioned `AUTH_EMAIL_FROM` and encrypted secrets during future deployments. Check legitimate delivery failures, 429/503 trends and inbox placement during the pilot; follow the onboarding runbook limits. Do not send third-party test invitations or alter credentials without authorization.
2. Use the production administrator account to prepare seasons, students, and assignments. Enable Team Practice through the coach UI when needed and prepare a coach-reviewed question bank; the fresh academy default remains disabled.
3. Keep the 20-room/200-player regional load gate open until measured successfully. The production rejection guards do not add post-domain 101/full-match evidence. Do not attach live log tails during active matches; they may replace a Durable Object runtime.

## Evidence index

- [Cloudflare staging audit](docs/audits/2026-09-10-cloudflare-staging.md)
- [DNS cutover, review, and rollback](docs/operations/erudoza-cloudflare-dns.md)
- [Native Cloudflare operations](docs/operations/cloudflare-native.md)
- [Administrator recovery](docs/operations/native-admin-recovery.md)
- [Cloudflare port implementation plan](docs/superpowers/plans/2026-09-10-cloudflare-free-port.md)
- [Production Worker configuration](apps/web/wrangler.production.jsonc)
- [Production administrator/library smoke](.local/deployment/production-smoke/library-report.json)
- [Production edge, certificate, redirect, and frontend hash evidence](.local/deployment/production-edge/latest.json)
- [Encoded-path/query redirect probe](.local/deployment/production-edge/encoded-redirect.json)
- [Final HTTP-to-HTTPS and no-loop verification](.local/deployment/production-edge/https-upgrade-latest.json)
- [Production WebSocket rejection guards](.local/deployment/production-ws-guards.json)
- [Contrast fix audit and browser evidence](docs/audits/2026-09-11-contrast.md)
- [Deployed contrast HTML/JS/CSS hashes and API health](.local/contrast-audit/production-latest.json)
- [Coach onboarding configuration, protection limits and release evidence](docs/operations/coach-onboarding.md)
- [Local onboarding browser evidence](.local/coach-onboarding-browser/report.json)
- [Production onboarding release smoke](.local/deployment/onboarding/production-smoke.json)
- [Resend sender DNS and unavailable-onboarding evidence](.local/deployment/onboarding/sender-preparation.json)
- [Authorized setup-email acceptance](.local/deployment/onboarding/delivery-preflight.json)
- [Production email activation smoke](.local/deployment/onboarding/activation-smoke.json)

## Honors design and artwork checkpoint — September 11

- Scope: [implementation plan](docs/superpowers/plans/2026-09-11-daily-training-progression.md), [design specification](docs/superpowers/specs/2026-09-11-daily-training-progression-design.md), [Coach recommendations](docs/product/2026-09-11-coach-workspace-recommendations.md), [archived student composition](docs/product/mockups/2026-09-11-daily-training.html), and [original artwork package](docs/brand/2026-09-11-original-assets/README.md).
- User decisions: daily practice/rewards; current student concept approved; SDA Pathfinder theme requested; embroidered patch family approved; refined hero removes the sun and improves compass detail; transparent embroidered flame-and-Bible logo explicitly approved; achievement feature named Honors System. The artwork package preserves exact prompts, 19 source masters with hashes, and the selected/archived status.
- Validation: coordinating-agent plan self-review aligned naming, API compatibility, event-day credit, concurrent first-credit tests, current migration ownership, and approved art/motion with the spec. All 63 local Markdown links resolved. All 19 master hashes matched their manifest; dimensions/pixel format/corner alpha inspected. Archived mockup/gallery inline scripts parsed. Gallery rendered at 1440/390/320 with all 10 current images loaded and no page overflow; 48/96/160 honor views and alternate surfaces checked. Pointer positions produced opposite signed rotations within 7 degrees; device reduced-motion preference reset artwork to rest. Full touch/coarse and React lifecycle tests remain implementation work.
- Limits: these are documentation, original-art, and local-preview checks. No application test suite, database fixture, migration, production upload/deployment, or live gameplay check was performed by this design task. Coach recommendations use current source and earlier local contrast screenshots, not a fresh live Coach session. The existing load gate remains open.
- Coordination: onboarding owns its code, deployment, and migration `0003_coach_onboarding.sql`; the future Honors migration number is planned as 0004 and must be rechecked at execution. Onboarding checkpoint `c7c5a10` was pushed before this design checkpoint. Only the listed design paths and this scoped progress update belong to this task.
- Git gate: plan, original artwork, archived mockup, gallery, and scoped progress update committed as `a07aba6` (`docs: plan Honors system and approve Pathfinder artwork`); pushed successfully to `origin/codex/cloudflare-free-port`. This is a documentation/artwork checkpoint, with no Honors application deployment. Next action: follow the saved student implementation plan when implementation is requested; Coach layout and further completion/Team Practice/season patch assets remain separate recommendations.

## Public entry design scope — September 11

- Added the [public landing and account entry brief](docs/product/2026-09-11-public-entry-design.md), cross-linked from the existing specification, implementation plan and asset README. Plan Tasks P1–P3 cover concrete page previews, approved composition integration and public/account/shared-brand verification.
- Scope includes `/`, `/login`, `/signup`, `/forgot-password` and `/join-coach`. Reuse the approved patch logo, no-sun landscape and Coach art; keep form controls readable and honor tilt limited to honor artwork. Preserve coach/student entry, native capability fallback, service-unavailable/retry states, verification and invitation flows, session handling and 300px security-widget space at 320px.
- Validation: coordinating-agent spec/plan self-review and an independent read-only public/auth source audit; 34 local Markdown links and nine explicit source paths resolved, existing test references checked, and staged Git whitespace review passed. These are documentation checks, not new public mockups, application tests, browser evidence, email activation or deployment.
- Git gate: the five scoped documentation files were committed as `b9fb19c` (`docs: include public landing and account entry in design plan`); push succeeded `75d3b3b..b9fb19c` to `origin/codex/cloudflare-free-port`. This follow-up log records the confirmed result.
- Next action: produce the public-page preview in Task P1 when that design work is requested; existing patch-family/logo approval remains valid.


## Honors implementation gate — September 11

- User authorized implementation after approving the daily-training interaction, patch family and logo, naming it the Honors System, and including public landing/sign-in/signup. Work is isolated at `.worktrees/honors-public` on `codex/honors-public`; the main checkout and original API 5080 remain untouched.
- Implemented paired native/.NET atomic day/week/mission/Honors evidence, idempotent starts, frozen recap and bounded read endpoints, additive migration 0004/EF migration, export mappings, Training HQ/weekly calendar, six-Honor collection, passage journey, selected-season recap recovery, shared pointer tilt, optimized approved artwork and public/account compositions. Coach layout/aggregation remains a documented follow-up; shared branding applies now.
- Local gates: full web/native Vitest: 486 passed/1 optional skip with maxWorkers=2; final .NET: 252 passed/1 optional load skip; exporter: 9 passed with all native migrations and populated Honors import/resume/replay. Both type checks, ESLint, canonical format verification and both production builds passed. Native Today: 22 queries/654 reads/0 writes at 100 vs 10,000 historical attempts with the full canon. Initial unrestricted Vitest found two stale expectations and a five-second Store test timeout under concurrent runtimes; corrected assertions/bounded workers passed without timeout changes.
- Independent backend/UI reviews closed seven concrete P2 findings with regressions. An eighth legacy-journey labeling defect was reproduced and corrected after the frozen browser run; six focused journey tests, lint/type checks and both builds passed afterward. Exportreview closes populatedHonorsresume gap; unearned scope-label wording differs but DTO shape/counters and earned evidence agree. Full native browser: 6 passed (6.5 minutes), including ten-player 5v5 and 17 Honors answers/scope recovery. Public/account isolated run: 55 views passed at 1440/390/320, including actual HonorArtwork pointer, reduced-motion and coarse-touch behavior. Full canonical browser: 36 passed and two expected opt-in audit skips (14.5 minutes), including desktop and Pixel 7 complete ten-player matches. Browser gates precede the final legacy-only presentation correction; the final post-correction full Vitest rerun passed 486 tests/1 optional skip.
- Coordination: fast-forwarded already-approved email source/operations through `b291af4` into this branch. Preserves activated sender and current production configuration. No production migration/deploy/account/email performed by this Honors task.
- Evidence: [implementation audit](docs/audits/2026-09-11-honors-implementation.md), [prepared release procedure](docs/operations/honors-release.md), and original art manifest. Detailed ignored reports live under `.superpowers/sdd/2026-09-11-daily-training-progression/`; browser/database artifacts stay ignored.
- Final source gate: all 118 scoped files staged and reviewed; staged whitespace check passed, all 19 original and 20 derivative hashes verified, no high-confidence secret patterns found. Final native build JS SHA-256: 8629f0914e07f322b4195e48048b101cc5c7b233c71d6b0b05df506f6a91cf2e. Git gate: implementation committed as `c569f2c` (`feat: add Honors system and approved patch artwork`) and pushed successfully to the already-approved private `origin/codex/honors-public`. The source push created the task branch and configured its upstream. No merge or production deployment. This follow-up records that confirmed result. Next: review the working local implementation; use the prepared release procedure for any later authorized deployment. Deployment remains a separate final reviewable action.

## Update convention

At each gate, refresh Current state/Next actions and add the result to Gates and evidence. For repository checkpoints, record the branch, commit identifier or message, and confirmed push result in the next log update; a commit cannot contain its own hash. Never mark an attempted operation successful without readback or command evidence. Retain historical failures with their resolution instead of silently changing their meaning.

## Patch motion and approved layout restoration — September 11

- User reaffirmed the daily-training mockup at port 4317 as visual authority after identifying drift. Restored compact learner header/shortcuts, mission/action/weekly hierarchy, compact passage tiles, Honors collection and recap composition. Actual saved evidence, season routes, legacy/early-completion cases and Coach workflows remain covered.
- Every approved patch now uses shared pointer lift/dip and soft contour shadows, including brand logos across public/account/Coach/student and recap artwork. Stationary bounds and reduced-motion/touch/lifecycle behavior are verified. Independent review found and closed the active-shortcut unpin/reorder visibility regression.
- Fresh local checks: 288 frontend tests across 40 files, ESLint, web/native TypeScript, native and canonical builds passed. Final native browser round: 27 views at 1440/390/320, 10 motion checks, six corner-placement captures and nine separate actual-image/side/fade route checks. No page overflow or JavaScript errors. Build size/dependency advisories and harness corrections are documented in [the audit](docs/audits/2026-09-11-patch-motion-and-layout.md). No new backend/full-gameplay suite or production deployment was performed for this UI refinement.
- New original artwork is proposal-only: embroidered Erudoza lettering, panoramic placement of the approved landscape, and four distinct backgrounds alternating left/right by page. The user explicitly requested seeing these before application. Masters/prompts/preview derivatives are in docs/brand; the [approval gallery](docs/brand/2026-09-11-patch-expansion-review.html) offers nine page assignments. No proposal is imported into app source/public assets. The lettering still needs real transparency preparation after visual approval.
- Preview: original mockup 4317, artwork approval 4318, corrected in-memory app 8887; original 8889 and main checkout are untouched. Next action: get user approval for the proposed artwork set/page mapping, then integrate only approved proposals. Layout and patch-motion source is locally verified; Git checkpoint/push follows this entry.
- Git gate: implementation, artwork proposals and validation record committed as `223d0b9` and pushed successfully (`df571c1..223d0b9`) to the already-authorized private `origin/codex/honors-public`. Repository privacy and ADMIN access were freshly verified. This source checkpoint does not deploy the app or approve the proposed artwork.

## Approved artwork application — September 11

- User approved the reviewed lettering, panorama and complete four-image corner family. Integrated `LandscapeBanner` and `PathfinderBackdrop` across public/account/Coach/student/fallback surfaces on isolated `codex/honors-public`. Four optimized corner copies match their approved source hashes exactly (400,526 bytes total). Nine approved route assignments alternate left/right and remain stable across filters/seasons. The panorama keeps its complete 3:1 composition above HQ/public content and beside desktop sign-in; mission/sidebar/weekly/passage ordering remains intact.
- Fresh local gate: frontend Vitest 313 passed across 41 files; ESLint, web/native TypeScript and both builds passed. At 18:49:35 UTC, the isolated native preview passed 33 views at 1440/390/320 with actual image/side/fade/HTTP checks, no overflow/broken images/JS errors, and 12 patch-motion checks including reduced motion and coarse touch. Goal/Honor focus restoration and active-shortcut visibility passed. Independent source review found no actionable issue; inspected screenshots confirm corner blending and readable panels/forms. See [integration audit](docs/audits/2026-09-11-approved-artwork-integration.md).
- Lettering is visually approved but not applied: the built-in transparency attempt again returned RGB with a baked checkerboard. The rejected output was not installed. Asked the user to permit local background removal, as required by the image-editing tool's alternative-method rule; that question remains pending. Original approved lettering/prompt are preserved, with the failed preparation attempt documented. Next: resolve that method choice, prepare real alpha, integrate through `PatchArtwork`, and check compact headers/legibility.
- Preview is at port 8885 with synthetic isolated data. Original 4317/4318/8887/8889 and main checkout remain available. No real email, hosted account/data write, migration, merge or production deployment. Browser evidence is ignored; full backend/.NET/gameplay and regional load gates were not rerun for this presentation-only change. Verified banner/corner source checkpoint and push follow this entry.
- Additional enabled-account gate: 15 views at 1440/390/320 passed for signup/recovery email/code and invitation missing-token states. An exact 300px local widget fits the 304px form at 320px; no overflow/image/JS errors, solid paper form surfaces and correct art assignments. Two code receipts and the widget were intercepted in the browser, with no email/account/password completion. Optional font network blocking triggered one harness-only assertion, documented with its corrected final run in the audit.
- Git gate: 25 explicitly staged files passed whitespace review and were committed as `f979ede` (`feat: apply approved Pathfinder banners and page corners`). The destination was freshly confirmed PRIVATE with ADMIN access; push succeeded `57c3bc6..f979ede` to `origin/codex/honors-public`. This is a source checkpoint, with no merge or deployment. Lettering transparency remains the separate pending method decision described above.

## Embroidered lettering and mobile Coach header — September 11

- The user approved local background removal, resolving the preceding lettering method blocker. The approved RGB source remains byte-identical. Reproducible preparation, a real-alpha master and manifest are saved under `docs/brand/2026-09-11-patch-wordmark`; optimized 320/640px WebPs are used by `ErudozaWordmark` through shared `PatchArtwork`. All opaque master pixels retain original RGB. Navy/ivory/white surfaces and compact headers were visually inspected; the name remains accessible without duplicate announcements.
- During the preview, the user reported the mobile Coach bar's clipped Seasons shortcut and oversized All sections control. Coach phones now retain two complete shortcuts including the current destination, compact 44px search/menu targets and menu-based pin management. Saved order, desktop pins, nested section switching and the approved learner strip are preserved.
- Final local source gate: 320 frontend tests across 41 files, full ESLint, web/native TypeScript and native/canonical production builds passed. Six mobile regression cases were added. Independent asset/source and mobile navigation reviews found no actionable defects. Native JavaScript SHA-256: `2c24917957580cdf47198a16363d5cf2f12e92b5cddfd1cdf89dd527dac40b1c`.
- Browser evidence: lettering integration passed 33 views at 1440/390/320 and 19 patch-motion checks, plus 15 enabled-account views with local browser substitutes. After the Coach refinement, the final build passed 27 Coach/student views and seven navigation flows at **19:21:25 UTC**, with complete labels/targets, focus restoration, retained pins/current section, no page overflow or JavaScript errors. The earlier account/lettering suites were not repeated after the Coach-scoped adjustment. See [the follow-up audit](docs/audits/2026-09-11-wordmark-and-mobile-header.md) for precise gates and ignored report locations.
- The loopback preview remains at 8885 with synthetic isolated data. No hosted data/email/account action, migration, backend/.NET/gameplay rerun, regional load test, merge or production deployment was performed for this presentation refinement. Existing nonblocking bundle-size/dependency advisories remain. Next action: review and checkpoint these scoped changes, push the authorized private task branch, and use the updated local preview; deployment remains separate.
- Git gate: 17 explicitly staged files passed whitespace/source review and were committed as `e576332` (`fix: simplify mobile Coach header and apply stitched lettering`). Push succeeded `fc7b1c1..e576332` to the already-authorized private `origin/codex/honors-public`. The updated 320px Coach capture is available with the local preview. Next: use the approved local implementation; any merge or deployment remains a separate action.

## Text wordmark correction and PVP design follow-up — September 11

- The user requested returning Erudoza to text because the stitched name did not look good in the app. Restored the shared text name, removed its image-only CSS, and retained the approved flame-and-Bible patch and shared motion. Source masters/derivatives remain unchanged as archived design history; DESIGN.md, the wordmark README and artwork gallery record the latest decision.
- Local verification: 24 focused wordmark/navigation tests, ESLint, web/native TypeScript and both production builds passed. Twelve views across landing/sign-in/Coach/student at 320/390/1440 confirmed visible text, loaded patch emblem, no lettering image, no horizontal overflow or JavaScript errors. Actual 320px Coach screenshot inspected. Evidence: ignored `.local/text-wordmark-check/report.json`, completed **19:33:07 UTC**. Native JavaScript SHA-256: `61e029e01985a6923b5a563c93df3c2f013c11dee2e1cf8949639236dec0abf7`. Full frontend/backend/gameplay suites were not repeated for this text restoration.
- The user asked whether mobile navigation should move to the bottom. Recommended stable four-item bars: student HQ/Study/Honors/More and Coach Overview/Seasons/Students/More, keeping task controls separate and accounting for safe-area spacing/support widget overlap. No bottom navigation was applied to app source.
- The user also requested substantial PVP UI work and patch artwork. Read-only source audit identifies current hub/lobby/live/review/completion states and role/score/phase constraints. Preparing a review-only interactive design and original team patches, following the user's request to see new art before application. Existing Team Practice logic and production data remain untouched. Next: checkpoint the verified text correction, finish/render the PVP preview, and show design/assets for approval.
- Text correction Git gate: committed eight explicitly staged files as `91b607c` (`fix: restore Erudoza text branding`); push succeeded `c0cf38a..91b607c` to the authorized private `origin/codex/honors-public`. The text name is in the local 8885 preview; this source push did not deploy it.
- PVP design gate: [interactive review](docs/product/mockups/2026-09-11-team-practice.html) now covers Hub/Lobby/Live match/Results/Artwork with Student/Coach views and mobile bottom navigation proposals. [Three original team patches](docs/brand/2026-09-11-pvp-patches/README.md) preserve existing mountain/Bible identity, real alpha, source prompts/masters and hashes. The [redesign brief](docs/product/2026-09-11-team-practice-redesign.md) records gameplay constraints, the five remaining Team Honor assets and implementation/verification sequence. No PVP or bottom-nav app source changed.
- Final review verification: 30 views at 1440/390/320, five patch-motion checks and eight simulated flows passed at **19:49:55 UTC**, with no overflow, broken images or JavaScript errors. Independent review's Coach phase/judgment/moderation and format issues were corrected and exercised in the final run. JavaScript syntax/whitespace and 31 Markdown targets passed. See [review audit](docs/audits/2026-09-11-team-practice-design-review.md) for evidence and limits; this does not establish a real match or deployed feature.
- Review is open at `http://127.0.0.1:4318/docs/product/mockups/2026-09-11-team-practice.html` in a retained in-app browser tab. Next: user review of the proposed PVP design, three patches and bottom navigation, followed by implementation and remaining Team Honor art within the approved direction. New assets remain proposal-only, honoring the request to see them before application. Design checkpoint/push follows this entry.
- PVP review Git gate: 16 explicitly staged proposal/evidence files passed whitespace review and were committed as `c6559ba` (`design: preview Team Practice game UI and patch artwork`). Push succeeded `91b607c..c6559ba` to the authorized private `origin/codex/honors-public`. This saves the review without applying PVP design/assets to app source or deploying them. User design/art approval remains the next action.

## Team Practice implementation in progress — September 11

- User approved the interactive Team Practice preview, the three core embroidered patches and the shown mobile bottom navigation, while requesting improvement to Room setup & Invitations. The original mockup stays the visual reference; work remains isolated in `.worktrees/honors-public` on `codex/honors-public`.
- Hub/room components, capacity-aware invitations and stable role-specific mobile destinations are implemented using real room state and shared controls. Approved core art has hash-verified 256/512px derivatives. Erudoza remains text.
- First integration checks: 346 frontend tests, ESLint, web/native TypeScript and native build passed. Subsequent review found appeal-draft loss during phase transitions and missing terminal rosters; these are being corrected before the final browser gate. The current source is not yet declared verified or checkpointed.
- Five new Team Honor designs were generated and shown for separate approval. Two masters have alpha; three returned baked checkerboards on both original and transparency retry. Visual approval and local-cleanup permission are pending. They are not applied to the app.
- Next: finish the review corrections, verify real native/canonical gameplay and responsive Coach/student layouts, record precise evidence, then commit/push the scoped task branch. No deployment or hosted-data action is part of this UI pass.

## Team Practice and mastery profiles — implementation gate in progress, September 11

- The user approved the Team Practice mockup except for requested setup/invitation refinement, then requested earned mastery Honors as profile images and consistent identity across platform/PVP messaging. Implementation is isolated in .worktrees/honors-public on codex/honors-public; main checkout and production remain untouched.
- Implemented approved PVP composition, setup/invitation feedback, stable mobile dock and text wordmark, core patch motion/shadows, shared profile picker/identity placement, immutable mastery-v1 evidence and paired native/.NET APIs. Earlier solo/PVP awards remain Practice milestones. Five new Team Honor art proposals remain unapplied pending their separate approval and cleanup-method decision.
- Completed source gates: .NET267passed/1existing optional load skip; exporter13passed including populated all-migration native profile readback; web/native type checks, full ESLint, formatting/EF parity and both builds passed. Independent reviews closed appeal-draft/terminal-roster, auth cache race, newly-earned cache, polling-finalization, rule-version, due-proof and audit-evidence issues. Full final combined tests and final profile-aware browser matches are running; do not treat their started state as passed.
- Current evidence and exact limits: docs/audits/2026-09-11-team-practice-and-mastery-profiles.md. Earlier fresh PVP recovery passed24roomviews/5flows and12shellviews/2flows/6motionchecks. Profile presentation uses a synthetic earned fixture; actual earning is covered separately by backend and full-match checks. Next: complete final local browser/source gates, review and explicitly stage scoped changes, commit and push the approved private task branch. No merge/deployment.

## Team Practice and mastery profiles — final local gate, September 11

- Final combined suite:579passed/1existing optional workload skip; .NET267passed/1optional load skip; exporter13passed. Web/native types, full ESLint, .NET formatting/EF parity, both final builds and staged-source hygiene passed or are checked before the checkpoint below. Six installed core-patch outputs and all ten proposal master/review hashes matched their manifests.
- Final native browser2/2passed(6.6minutes); canonical4/4passed(13.6minutes), including three completed ten-player/ten-question5v5 matches across the two runtimes, actual mastery earning by scribes, avatar save/reload and403for ineligible selection. Canonical produced36responsive captures. Preserve the recorded SignalR negotiation and Windows socket teardown diagnostics; these runs are not console-error-free claims.
- Final profile preview passed27views at1440/390/320,3identity/resetflows and3motionchecks at21:21:53UTC; representative screenshots inspected. Corrected measured320px long-Coach-name overflow with text wrapping, then rebuilt both targets and reran the profile pass. The profile fixture is synthetic; independent gameplay/integration tests prove earning. Its moderated-chat screenshot shows Reconnecting, so its passing identity/layout checks are not sustained WebSocket evidence.
- Review findings are closed, original practice milestones preserved, and new Team Honor proposal images remain unapplied. The screenshot was queued in the Codex side panel; computer-use reconnection failed, so final in-app interaction was not verified. Local preview remains8885/8886. No hosted data/email/account operation, migration, merge or deployment. Next: explicit source checkpoint and authorized private-branch push, then record their confirmed result.

- Git gate confirmed: the 102-file reviewed implementation was committed as 98ab848 (feat: add mastery Honor profiles and approved Team Practice UI). Staged whitespace and high-confidence secret scans passed. Push succeeded f48b913..98ab848 to the freshly verified PRIVATE/ADMIN origin, https://github.com/Victor-CS-Core/Erudoza-BibleCompetition.git, branch codex/honors-public. The worktree was clean after the push. Final native JavaScript SHA-256:78dfa6ee9f473cbebf05a50e1323aef2bb9a356aaba08c554eac0e4f65fcf9f7. This follow-up records the confirmed source checkpoint; no merge/deployment. Next: review the local implementation and separately resolve the five Team Honor art approvals before applying those new designs.

## Approved Team Honor release preparation — September 11

- The user explicitly approved all five Team Honor designs and instructed commit, push and production deployment. This supersedes earlier pending design/release approvals. Root owns production mutations; independent preflight and browser-preparation agents made no production changes.
- Prepared both shared consumers for 256/512px WebPs, including the legacy milestone display through shared pointer motion/shadows. The scoped source gate passed 28 tests, web TypeScript, scoped ESLint and whitespace. Ten final image outputs do not yet exist; these consumer changes are uncommitted and are not a release gate. The approved originals and review copies are unchanged.
- The three baked checkerboards still require extraction. Built-in image edits failed while opening the Windows source paths because the sandbox helper could not set directory permissions (access error 5). The separate method question asks permission for local background cleanup; no answer has arrived. A reproducible, original-hash-guarded preparation script is written and syntax-checked only. No fallback image edit has been executed.
- Read-only production preflight at21:42UTC found current Worker1e9d199c-3562-4706-897d-daf92e46e05f at100%, matching native bindings/public vars, exactly additive migration0004 pending, no conflicting training-start duplicates and zero projected rooms. Room projection is asynchronous; recheck immediately before release. Anonymous health/Me401/landing/login/signup checks passed. Deployment-level edge settings were source-checked, not independently retrieved.
- A private pre-release D1 export is saved under ignored .local/deployment/honors-release-20260911. Its24,765,575bytes hash to7708ca613020146fa46d78d815ca8c0f665685cf08cdbb55b27f8f3c2dcafc74. An isolated in-memory SQLite import passed integrity_check with10tables. The initial disk import was stopped after verifying its owned process because per-statement disk commits were slow; the successful in-memory check used the same export. Backup contents/private recovery details remain outside source control.
- Prepared ignored responsive asset and production smoke harnesses. The asset harness is syntax-checked, not browser-run: planned ten exact served hashes, six views at1440/390/320 and15hover/reduced-motion/touch cases. The earlier complete native/canonical gameplay/profile gates remain the backend evidence; no fresh full match is claimed for this asset pass.
- Next: receive the local-cleanup method answer, produce and inspect true-alpha derivatives, run final source/build/browser gates, commit and push codex/honors-public, recheck live state, apply0004 and deploy with wrangler.production.jsonc, then record live smoke and a pushed evidence checkpoint. No production migration/deployment, new hosted account, message or email has occurred in this release preparation.

## Approved transparent Team Honors — final local gate, September 11

- The user explicitly authorized local background removal, resolving the only remaining method blocker. All five original masters and five archived review images remain byte-identical. Three prepared true-alpha PNGs preserve every fully opaque RGB pixel; all ten256/512px WebPs match their manifests, have transparent corners and total763,268bytes. Navy/ivory/white contact sheets and representative app screenshots were inspected.
- Shared collection/profile/avatar and legacy milestone consumers now use the approved images through PatchArtwork. Earning rules, unlock checks and saved identity semantics are unchanged. The two-consumer source gate passed28focusedtests/webtypes/scopedESLint/whitespace. Both final production builds and native/web TypeScript passed. Existing bundle-size/dependency advisories remain.
- Fresh final browser evidence:27profileviews at1440/390/320, three identity/resetflows and three motionchecks passed22:03:08UTC; dedicated new-art run passed10servedhashes, sixresponsiveviews and15hover/reduced-motion/touchcases at22:04:25UTC. No page overflow, broken images or JavaScript page errors. Initial asset-harness CSS normalization failure was corrected in the ignored harness and the full run passed without app changes. See docs/audits/2026-09-11-approved-team-honors-release.md.
- Native production dry run passed with72assets and the existing D1/threeDO/rate-limit/sender/origin/Turnstile configuration. Fresh remote readback still shows Worker1e9d199c-3562-4706-897d-daf92e46e05f, only0004pending, zero Playing room projections and main maintenanceb291af4included. Private repo/ADMIN access and exact origin were reverified. Backup integrity remains verified.
- Automatic approval review rejected creating a production smoke script that would read the private admin credential file, because preflight prohibited credential reads. No retry/read occurred. Credential-free live smoke is prepared; authenticated production coverage remains unavailable, with authenticated local suites reported separately.
- Next: explicitly stage and commit the reviewed assets/source/docs, push codex/honors-public, apply0004and deploy the paired native Worker/assets, then verify live hashes/health/pages and record a pushed evidence checkpoint. No default-branch merge or synthetic production data is authorized or needed.

## Approved Honors production release — September 11

- Source gate confirmed:24explicitlystagedfiles passed whitespace/private-artifact/high-confidence-token checks and were committed as838a066(feat:applyapprovedtransparentTeamHonorpatches). Push succeeded0d9c7fc..838a066to the freshly verified PRIVATE/ADMIN origin/codex/honors-public. No default-branch merge.
- Applied only reviewed native migration0004_training_progression.sql after the private backup; readback confirms no pending migrations. Deployed the paired Worker and72-file native asset directory using wrangler.production.jsonc. Deployment uploaded55new/modifiedfiles and completed at22:08:10UTC. Worker `257f14ee-6d2d-4896-9319-de0f4fc283f2` is verified at100%traffic; rollout ID54feb062-2ac5-4131-8da1-ff62bac50a03. Existing database, threeDOs, sender, public origin, Turnstile and rate limits were retained.
- Live credential-free smoke passed22:08:47UTC: healthy nativeAPI/D1, anonymousMe/profile/identity401guards, landing/login/signupavailability, trustedTLS/DNS/wwwredirects, exact publishedJS/CSShashes and all10approvedTeamHonorWebPhashes. JS SHA2562176796a05640df8353002ddee3718d43076e59cc4a4e1d4a0d8c84010e87251. Private fullreport remains ignored under .local/deployment/honors-release-20260911/production-edge.
- Limits: authenticated production login/profile/gameplay were not executed because the proposed credential-reading harness was rejected by automatic approval review; no credential read/retry or production fixtures were used. Local33responsiveviews/18motionchecks and prior complete authenticated native/canonical gameplay remain separate evidence. Regional20-room/200-player capacity remains unverified. No real messages, emails or new hosted accounts were created.
- Release is complete and live. Rollback, if ever needed, can restore the preceding reviewed compatible Worker1e9d199c-3562-4706-897d-daf92e46e05fwhile retaining additive schema/new records. This evidence checkpoint is being committed and pushed after the confirmed release.

## Main aligned with production — September 11

- The user explicitly requested updating GitHub main to reflect the deployed application. Fresh fetch showed main at1a370cc with zero unique incoming commits and the release branch32commits ahead; the working tree was clean. Main is the repository default branch and no worktree had it checked out.
- Verified838a066(deployedsource)throughcc55a04(releaseevidence)differs only in PROGRESS.md and two release documents. Application/API/frontend/build/configuration files match exactly. Existing local/browser/live release gates were reused because this integration changes no application code. No additional application suite or redeployment was performed for the branch fast-forward.
- Reviewed CI and independently checked GitHub settings: main is unprotected, rulesets/webhooks/deployment records were empty, and the sole active workflow runs tests/builds/formatting/loopbackE2E/Bicepcompile with no release/deploy step. This does not rule out an independently configured external provider integration.
- Normal non-force push advanced origin/main1a370cc..cc55a04; live ls-remote confirmedcc55a0424afc2344fdd6da0c265f21da5e3d1121. Local main was advanced with an expected-old-value guard. Existing worktrees, task branches, private artifacts and running previews were preserved.
- This documentation-only follow-up is committed and pushed to main and codex/honors-public together. Final verification compares GitHub main with HEAD and checks that only documentation differs from the deployed source. GitHub CI is separate from the already-passing local/live release evidence; do not claim its new run passed before completion.

## Training replayability assessment — September 11

- User requested a review of training replayability for memorizing season-assigned chapters and further gamification recommendations. Reviewed `6be36f0`; application files match deployed source `838a066`. Work is on `codex/training-replayability-audit` in the current Linux checkout. No application behavior, hosted student data, migration or deployment changed.
- Fresh local evidence: 152 existing tests passed across 20 files; six additional isolated native audit probes passed. The probes completed 98 sessions with 759 correct answers, plus three answers demonstrating that a correct reference choice postpones a failed wording review by 48 hours. A controlled 28-day course exercised daily Review/Practice with synthetic actors; it is not real student learning or production authentication evidence.
- Findings: eight-verse consecutive drills repeated the same verse/activity mapping for 16 sessions; Advanced produced wording evidence on only three verses and reference evidence on two. Daily reviews improved coverage: the simulated Daniel 1 course reached wording/reference evidence on all 21 verses. Advanced masks remained unchanged in 20 of 21 sampled verses; a 47-word Verse Builder required 281–449 adjacent moves in 20 deterministic shuffles (median 375). Difficulty ceilings and fixed-size Honor requirements also need clearer attainable next actions. These findings remain open; none is described as fixed.
- Proposed order: repair passage/skill selection and review scheduling, simplify long-verse input, then add chapter expeditions, verse chains, delayed chapter checkpoints, comeback quests and cooperative season rewards. Preserve coach scope/difficulty control, deterministic cards and historical Honors. Details, source references, research and future verification gates are in [the audit](docs/audits/2026-09-11-training-replayability.md).
- Limits: live public homepage verified in the browser; direct anonymous command-line reads returned 403 in this environment. No authenticated live training, fresh deployed hash check or student usability study. Canonical source inspected; .NET execution unavailable because no SDK is installed. Audit harness clock-isolation and signed-zero assertion issues were corrected before its final passing run. No application fix was used to make the probes pass.
- Local assessment gate reached. Documentation/link/whitespace review and an explicit source checkpoint/push follow; the next product action is implementation of the prioritized learning-loop improvements when requested.
- Git assessment gate confirmed: the two documentation files passed staged whitespace review and all 18 local audit links resolved. Committed as `da0e136` (`docs: audit training replayability and chapter recall`); pushed successfully to `origin/codex/training-replayability-audit`, with remote readback matching `da0e136a4089e5d932af4e1c0534473b1b82398d`. Exact destination was freshly verified as the existing PRIVATE repository with ADMIN access. This follow-up records the confirmed checkpoint; no merge or deployment. Application/configuration diff against release source `838a066` is empty. Next: review the recommendations and implement the prioritized changes when requested.

## PBE standards alignment review — September 12, 00:13 UTC

- User clarified that replayability must prepare students for PBE testing. Verified the official NAD resources page and its currently linked 2023–24 How-to Guide, including question formats, grading, timing, team practice and standings. The page names six books for 2026–27 and says new season resources are forthcoming; this does not identify or alter the user's selected season. NAD is the assessed baseline, not certification of every regional variant.
- The [PBE crosswalk](docs/audits/2026-09-11-pbe-training-alignment.md) records source-backed requirements and concrete application gaps. Solo Simulation has no factual ShortAnswer provider, no partial-credit rubric or enforced response deadline, and still includes reference guessing and construction exercises. Team Practice supplies reusable rubric/timer/appeal behavior but adds speed points, stops at five students and repeats the first eligible bank entries. Numeric true/false limits are identified as app policy rather than quoted as an official percentage.
- Revised the earlier audit: competition questions and accurate rehearsal scoring now lead; reference games, verse chains and optional full-verse recitation support memorization without establishing PBE readiness. Delayed chapter checkpoints and comeback quests should test factual and exact-word knowledge using fresh source-backed questions. Historical Honors and match versions must be preserved.
- Fresh local verification: 24 existing tests passed across three native study/practice files in 312 ms; nine additional native function/command probes passed. They confirmed generator gaps, timing-table parity, early-answer bonus, point validation outside the published table, six-student rejection, repeated room sets, partial credit and strict spelling matching. The command boundary correctly rejects extra answer fields. Initial harness commands used invalid non-GUID IDs; corrected synthetic IDs produced the final passing run without an application patch. Temporary probes and downloaded source PDF remain ignored under `.local/pbe-standards/`.
- Scope and limits: standards audit and recommendations only. No application implementation, database mutation, authenticated live test, fresh .NET execution, merge or deployment. The local documentation gate is reached; explicit staging, commit, push and remote readback follow. Next: implement the versioned PBE question/rehearsal path and adaptive replay selection, then pass the crosswalk's acceptance gates before describing the app as aligned.
- Git standards-review gate confirmed: three explicitly staged documents passed whitespace review, 31 local audit links resolved, and the scoped credential-pattern check passed. Commit `c8823d9` (`docs: align replayability recommendations with PBE rules`) was pushed normally to the existing private `origin/codex/training-replayability-audit`; remote readback matched `c8823d979affcf509942ea60479f1ab8d811f224`. Application/configuration diff against deployed source `838a066` remains empty. This follow-up records that verified source checkpoint; the implementation and live-alignment gates remain open.

## Independent Team Practice assessment decision — September 12

- User explicitly requested adding automatic grading by default, optional later dispute review, and optional coach-led rehearsal to the assessment. Updated both training audits and replaced the earlier recommendation favoring coach-led initial delivery.
- Fresh source inspection confirmed that uncoached rooms already score and advance automatically; coached review waits until both answers are resolved. The accepted recommendation builds on the independent path and keeps matches completable/replayable without an adult online. It specifies rubric-based partial credit, conservative spelling handling, answer reveal only after both teams lock or time expires, non-blocking dispute review, provisional disputed scores and consistent later corrections.
- Added acceptance gates for completing/replaying without a connected coach, continuing after an appeal, preserving answer secrecy, and reconciling later corrections without duplicate rewards or blocked unrelated practice. This is documentation only; no application implementation, production operation or new application-test claim. Next: documentation verification and the required scoped checkpoint/push; implementation remains a separate next action.
- Decision checkpoint verified: 33 local audit links resolved, staged whitespace passed, and the application/configuration diff against deployed source remained empty. The three scoped documents were committed as `4e73828` (`docs: recommend independent grading and deferred disputes`) and pushed to `origin/codex/training-replayability-audit`; remote readback matched `4e73828f9184ebfa71593b95856608bea78f4fab`. This follow-up records the confirmed checkpoint. No implementation or deployment occurred.

## PBE implementation planning gate — September 12

- User requested an implementation plan based on both assessments. Created [the index](docs/superpowers/plans/2026-09-12-pbe-training.md) and four linked plans with 12 tasks, exact module/file boundaries, proposed contracts, concrete regression examples, validation commands and staged release gates. Both assessments link to the plan.
- The plan retains automatic grading and independent progression as defaults, optional deferred disputes and optional coached rehearsal. Phase A prepares source-verified question banks before play; B adds factual solo practice and evidence-specific scheduling; C adds accuracy-only timing, independent presentation, six-student rosters and corrections; D adds dated chapter progress/cooperation and integrated rollout checks. No runtime AI grader or paid service is required.
- Self-review covered all audit findings, legacy score/Honor preservation, short and excluded assignments, multiple books and commentary, fresh-question delayed proofs, failed recall versus recognition, answer secrecy, atomic attempt/evidence updates and replay determinism. Corrected proposed warmup evidence so easier cue patterns cannot silently relax existing Advanced Honor requirements. Rehearsal question-mix quotas do not prevent focused commentary learning.
- Planning validation: five plan documents passed header, local-link/anchor, code-fence and placeholder checks; the final scoped documentation validation is recorded with the checkpoint. Root SDK configuration is 10.0.303 with latest-patch roll-forward, but fresh `dotnet --list-sdks` returned no installed SDKs. Future canonical execution remains a prerequisite, not a claimed passing gate. Web Speech specification was checked for synthesis events/local voices; actual device support and pronunciation remain explicit execution tests with a disclosed text fallback.
- No app code, migrations, production content or configuration changed. No new application tests were run for these documentation edits; test commands and expected failures in the plan are future work. Next: commit/push the reviewed plan, then begin Phase A in an isolated implementation worktree when execution is requested. Local verification, pushed source and live release must remain distinct.
- Planning checkpoint confirmed: all eight scoped documents passed staged whitespace review; the five plans contain 12 unique tasks, and 53 local documentation links/anchors resolved. Header/code-fence/placeholder and scoped credential-pattern checks passed. Commit `83ebcf6` (`docs: plan PBE training and independent rehearsal implementation`) was pushed normally to `origin/codex/training-replayability-audit`; remote readback matched `83ebcf6c78cdf01c3e8902d00bbe23570f6fe925`. This follow-up records the verified checkpoint. Application/configuration diff against deployed source remains empty; no implementation or deployment is claimed.

## PBE implementation preflight — September 11

- Goal activated from the four-phase, 12-task plan; isolated implementation branch `codex/pbe-training` created from `6d0291a`. Execution uses task implementer/reviewer passes; the controller owns this log and branch checkpoints.
- Full clean baseline: web/native Vitest 579 passed/1 optional skip (28.84s); .NET unit 71 passed plus integration 196 passed/1 optional load skip. Native tests ran with bounded workers.
- Environment recovery: Sharp initially attempted a system-libvips source build; the documented `SHARP_IGNORE_GLOBAL_LIBVIPS=1` install succeeded. Official task-local .NET SDK 10.0.303 resolves the planning-time canonical-test blocker. Node 26 experimental webstorage caused 29 pre-existing AppShell setup failures; the documented opt-out passed both focused tests and the full clean baseline. No package/lock or system configuration changes.
- Limits: this is a local baseline and isolation gate, not new PBE behavior, a pushed implementation checkpoint, browser certification, deployment or pilot. Next: shared rubric implementation and regression/parity verification.

## PBE A1 rubric gate — September 12 UTC

- Implemented v2 private question/target/rubric contracts, public question DTOs, per-part weighted one-to-one grading and fixed PBE point/time limits in native and C#. Exact word order, configured variants and source/target coverage are validated; no fuzzy or generative grading. Question views omit answer keys and evidence before reveal. Legacy production scoring code is unchanged; dedicated regressions preserve its historical nine-point behavior.
- Local commits: `b8049ba` (versioned rubrics), `a771aa3` (exact PBE enum JSON). Independent spec/quality review found numeric enum JSON could be accepted by C# while rejected by native. Scoped converters and regressions closed numeric-token, numeric-string and lowercase-name mismatches without changing legacy global JSON settings; scoped re-review approved with no remaining findings.
- Verification: full web/native 587 passed/1 optional skip and full C# unit suite: 79 passed before final strict-field hardening. Final post-hardening native PBE/legacy suite: 11 passed and both TypeScript checks passed; after the enum fix, final C# PBE/legacy suite: 34 passed. Shared literal fixtures run in both runtimes. RED/GREEN evidence was retained locally. No claim of a fresh full solution run after the final fix; A1's focused parity gate is closed.
- Boundaries: HTTP command rejection and authorization are later integration tasks; A1 provides evaluator validation. No bank, migration, UI, gameplay, deployment or live-pilot gate is complete yet. Next: A2 scoped bank and additive projection storage, then A3 authoring/coverage.
- Checkpoint preparation: removed an accidentally tracked SDD report from the index while retaining its ignored local copy; it contains only implementation/test evidence. Tracked progress and phase checkboxes summarize the verified result. Push status will be recorded after readback.

- A1 pushed source checkpoint confirmed: `473782e6aeefac2f1ddb69a0e0f64cc83b460bc9` is the remote `codex/pbe-training` tip after successful branch creation/upstream setup. Working tree was clean on readback. This is a source checkpoint only; production is unchanged. Next: A2 bank/storage implementation.

## PBE bank work in progress — September 12 UTC

- A2 is implementing explicit season-scoped PBE bank endpoints and dedicated question/target record kinds, with equivalent C# PbeTrainingRecord storage. This keeps new questions out of legacy selectors and makes coach preparation independent of the organization Team Practice setting. Initial native HTTP and canonical HTTP/SQLite upgrade regressions passed; the gate remains open while source-fingerprint, pagination, concurrency and read-budget checks are completed.
- A2 read measurement found a tenant-wide scope/assignment fingerprint query grew with unrelated history. The query was changed to indexed branches; final counts and the same-season excluded-source case remain under verification. These are local synthetic measurements, not a live capacity claim.
- Source inspection also confirmed that the current range-only source/assignment model cannot represent commentary introductions without fabricated coordinates. The phase-A plan now includes A2b: explicit season-owned introduction packs and deliberate student assignments, stored additively in PBE records. A3 supplies source-review/assignment UI; B2/D2 consume the resolver and introduction groups. This is one additional task within the approved source-coverage requirement (13 total), not completed functionality.
- The refined plans passed unique-task, code-fence, local-link and whitespace checks. No app UI, real content, pilot, deployment or main merge has been performed. Root owns plan/progress edits; the A2 implementer owns its scoped product/test commit.
- A2 implementation commit `8250d23` is local. Full native/web verification passed 600 tests/1 optional skip after restoring the canonical library seed write budget. Full .NET verification passed 290 tests/1 optional load skip before final narrow self-review hardening; final focused PBE/migration checks cover those later changes. Independent review found two actionable concurrency gaps: C# source revalidation omitted citation/pack type, and native cross-season question identity was checked outside the atomic write. Fix round one is active; this gate is not yet reviewed or pushed.
- Before B1 implementation, refined the proposed scheduler so early successful replays preserve a future review date/interval; only a due recall review advances the interval. Failures reset immediately and the first unaided recovery schedules one day. This prevents immediate repetition from replacing delayed retrieval. The 1/3/7/14-day intervals remain an app pilot policy, not an official PBE rule. Five plans still contain 13 unique tasks; 19 local plan links, balanced code fences and whitespace checks passed after the refinement.

## PBE A2 bank gate — September 12 UTC

- Implemented season-scoped import/publication and counts-only student coverage in both runtimes, with default-disabled `pbeEnabled`, dedicated target/raw-question records and a newest-published head projection. Multi-source questions require every source in the authorized assignment. Mechanical reference/evidence checks bind to canonical source fingerprints; coach preparation remains independent of Team Practice opt-in. Legacy questions, saved rooms and Honors retain their contracts.
- Local commits `8250d23` and `d3cd069` passed independent spec/quality review and a scoped fix re-review. Reproduced and closed stale C# citation/source-kind validation, concurrent native cross-season question ownership, and native/C# Int32 question-version parity. The import race now gives one success/one conflict with no partial losing records. No open review findings remain for A2.
- Verification chronology: full web/native 600 passed/1 optional skip after the index write-cost correction; full .NET 90 unit + 200 integration passed/1 optional load skip before final narrow hardening. Final post-review focused suites: native 30 passed, canonical 8 passed, both TypeScript checks, lint and scoped C# formatting passed. No fresh full-suite claim after the review fixes. Actual SQLite upgrade/reapply preserved old room JSON and permanent Honor evidence; EF reports no pending model changes. SQL Server migration script generation was checked; no SQL Server execution is claimed.
- Private native bank measurement: 5,101 questions and targets took 20,502 reads/35 queries; adding 10,000 unrelated progress and 10,202 excluded-source projection rows changed this to 20,504/35. The season-only index preserves the existing canonical-library seed write budget of 93,510 on initial install/0 on replay. These are isolated synthetic budgets, not production capacity or public metadata-route totals.
- Boundaries: coordinate-free introductions are A2b; coach draft/target pages and conversion UI are A3; atomic persisted-session selection is B2/C2; exporter support is D3. No UI, deployment, live data or pilot gate is claimed. Next: verified branch checkpoint/push, then A2b.
- A2 pushed checkpoint confirmed: `763fbcd59d4ea5381156787ee4a867189b5327c0` was pushed normally to the existing private `origin/codex/pbe-training` and matched remote readback. Product source includes `8250d23`/`d3cd069`; the checkpoint also records the reviewed gate and source/scheduler refinements. Next: A2b implementation. Production is unchanged.

## PBE introduction work in progress — September 12 UTC

- A2b has initial native/C# implementations of immutable introduction packs, coach preparation/review, explicit member assignments, an assigned reader and a common source resolver. Synthetic introduction-only members work without a fabricated Scripture assignment. Initial native HTTP/migration checks (30 passed), canonical source/bank checks (5 passed) and both typechecks passed; malformed-input, revocation and concurrent-edit checks remain underway. This is not a completed review/checkpoint gate.
- A genuine repeated assignment request after a lost response reproduced a 409 and was corrected to return current state without duplicate rows. Scoped private-bank measurements remain within budget (20,506→20,508 reads/40 queries after unrelated/excluded growth); final budgets and compatibility evidence remain to be recorded.
- B2 integration preparation identified two concrete requirements: server-record active-card source assistance so a client cannot clear aided status, and include explicit introduction assignments in student season/daily discovery currently driven by legacy chapter assignments. These are recorded in the solo plan and execution notes; the UI/session work remains pending.
- Introduction implementation commit `1865e22` is local. Full web/native verification passed 610 tests/1 optional skip and .NET passed 90 unit + 210 integration tests/1 optional load skip before a final narrow Unicode validation correction; post-correction native 37 and canonical 16 focused tests, typechecks, lint and formatting passed. Independent review accepted the source/authorization/concurrency design but found a P1 request-budget defect: per-student validation and per-pack assignment reads grow beyond the deployed Free tier's [50-query invocation limit](https://developers.cloudflare.com/d1/platform/limits/). Fix round one is replacing them with set-based queries and adding complete HTTP budget tests. The A2b gate remains open and unpushed.

## PBE A2b introduction gate — September 12 UTC

- Implemented immutable, coordinate-free introduction packs, coach review, deliberate assignment to existing active season members, an authorized source reader and shared Scripture/introduction resolution in native and C#. No fabricated Scripture rows or automatic enrollment. Question publication and student access require the current selected book, approved licensing, source review and applicable explicit assignment. Source corrections create new identities; historical records remain intact.
- Commits `1865e22` and `9c6afcd` passed independent task review and scoped fix re-review; no remaining findings. The review's request-budget defect was reproduced and corrected with bulk reads/writes and a consolidated metadata load that retains fresh before/after scope checks. Lost-response retries are idempotent, stale changes conflict, and transaction-time member/source/coach revocation rolls back the complete replacement.
- Verification chronology: full web/native 610 passed/1 optional skip and C# 90 unit + 210 integration passed/1 optional load skip before the final narrow Unicode source validation fix. After that fix, native 37 and canonical 16 focused tests passed. Final native-only request-budget fix: 43 focused tests, both TypeScript checks and lint passed; no fresh canonical/full-suite claim for that native-only round. Existing seed-write budget and historical behavior remain covered.
- Full native HTTP measurement includes middleware/auth: replacing 500 students uses 14 binding calls/17 submitted SQL statements, identical retry 13/13, listing 51 packs 12/12; the 5,101-question bank with unrelated/excluded growth uses coach 41/student 39. A 500-student final-member revocation race and mid-load source revocation returned conflicts without partial writes or answer leakage. SQL-statement counting is a conservative local budget, not a live Cloudflare quota-enforcement or capacity test.
- Next: A3 coach source/target/rubric authoring and honest published coverage. Student session/daily navigation, assisted-attempt accounting, team play and exporter integration remain later tasks. No UI completion, real commentary content, deployment or pilot is claimed. Explicit documentation checkpoint/push follows this local reviewed gate.

- A2b pushed checkpoint confirmed: `d614e9ba26642553c870e318bb4a9d08d1c52d07` was pushed normally to the existing private `origin/codex/pbe-training` and matched remote readback. This includes reviewed source `1865e22`/`9c6afcd` and the documentation gate; production remains unchanged. A3 implementation has started from that tip.


## PBE authoring work in progress — September 12 UTC

- A3 native HTTP regressions reproduced the supported 100-question/500-target import exceeding the 50-statement local budget; the set-based implementation now uses 22 binding calls/24 SQL statements including authentication and guards. Standalone target declaration and bounded coach authoring reads pass initial HTTP checks, including student 403. These are initial implementation milestones, not a completed task review.
- Native Unicode-only evidence/prompt/label regressions passed after reproducing rejection mismatches; a canonical RED confirmed BOM-only rubric content was accepted. Canonical completion and UI source/target/introduction forms are underway. Student bank metadata remains counts-only. No deployment or live content change.

- Cross-task review refined two later contracts before implementation: B2 must distinguish disabled new-start admission from valid saved-session continuation, matching the rollback plan; actual scope/source/member revocation still blocks access. D1 now carries B1’s recall discriminator and requires recall-capable variants for delayed stamps, so true/false recognition cannot be promoted into chapter retention. These are plan refinements, not completed session/progress behavior.

- A3 canonical focused integration gate passed 47 tests/1 optional load skip. Browser preparation reproduced and fixed native coach bootstrap omitting season metadata when legacy Team Practice is disabled; canonical already retained it. Bounded authoring pages now distinguish the current published head from older published versions. UI/browser verification and independent task review remain open.

- A3 actual native browser journey passed source preview, standalone target-before-question, draft/save versus explicit publish, distinct-ID variant, immutable version correction and introduction review/member assignment. Captures at 1440/390 passed; the 320px check found 362px document overflow, now being fixed. Final conversion, season reset, role separation, complete source gates and review remain pending. No browser-completion claim yet.

- A3 browser milestone: native Chromium journey and canonical Chromium/Pixel 7 emulation journeys passed, with 1440/390/320 captures, keyboard/reduced-motion checks, explicit legacy conversion and coach-to-student access denial. This is browser/device emulation, not a physical mobile-device claim. Self-review reproduced same-season authoring-page read growth from a forced index (3→1,003 reads), fixed with the existing scope index (2→2), and corrected multi-source preview order. Full source checks/builds are running; commit/report and independent review remain pending.

- A3 local commit `c1b53b4` is ready for independent review. Final full web/native: 627 passed/1 optional skip; C#: 90 unit + 210 integration passed/1 optional load skip. Both typechecks, lint, both builds, scoped C# formatting and staged whitespace passed; existing bundle-size advisory remains. Final native browser 1 journey and canonical 2 journeys passed. Reviewer is active; A3 is not yet a reviewed or pushed gate.

- A3 review has identified a canonical member pagination mismatch: its text cursor predicate and Guid-value ordering differ under the supported SQL Server provider, risking skipped/repeated pages above 100 members. Native/SQLite browser results do not cover that provider ordering. The review is still finishing; A3 remains unapproved while findings are collected for the scoped fix round.

- A3 final review requests one scoped correction for canonical member pagination; the original implementer is addressing it with a GUID-order counterexample and focused multipage/provider-query verification. Review otherwise accepted the task’s authoring/security boundaries. Browser color-environment warnings are logged for D3 cleanup; physical-device, SQL Server runtime, production and real-source approval remain explicitly unverified rather than implied by emulated/SQLite checks.

- A3 pagination fix RED reproduced the SQL Server query translation mismatch in the exact production member query. A 205-GUID ordering witness plus actual three-page HTTP checks also exposed SQLite uppercase storage versus lowercase cursor comparison. The fix normalizes the same text expression for both predicate and ordering. No local SQL Server listener is available and Docker socket access is denied; evidence is actual SQLite HTTP behavior plus configured SQL Server query translation/SqlGuid ordering, not SQL Server execution. Final focused verification/commit and re-review remain pending.


## PBE A3 authoring and Phase A local gate — September 12 UTC

- Coaches can prepare current season sources and introductions, declare immutable learning targets before questions, preview factual/list/exact rubrics, save drafts and explicitly publish. Coverage distinguishes gaps, single question variants and actual published heads without claiming complete chapter knowledge. Deliberate legacy conversion creates a separately reviewed PBE draft and preserves original questions/history. PBE preparation remains available with legacy Team Practice disabled; students cannot read authoring data.
- Commits `c1b53b4` and `733e4f5` passed independent task review and scoped fix re-review. The member-page cursor now uses identical normalized GUID-text expressions for filtering and ordering, correcting SQL Server ordering and actual SQLite cursor-case defects. No remaining Important/Critical review findings. A browser color-environment warning is recorded for D3 cleanup; the existing build chunk advisory remains.
- Final initial implementation gate: web/native 627 passed/1 optional skip; canonical 90 unit + 210 integration passed/1 optional load skip; both TypeScript checks, lint, both builds, scoped C# whitespace formatting and source whitespace passed. Native Chromium and canonical Chromium/Pixel 7 emulation completed three real authoring journeys with 1440/390/320 layouts, keyboard, reduced motion, conversion, version preservation, season reset and coach-to-student denial. A measured 320px overflow was fixed before these final browser passes.
- Post-review C#-only fix: 17 focused PBE tests and scoped formatting passed. A 205-member, three-page HTTP regression verifies exact membership, exclusions, cursor casing and exhaustion. SQL Server evidence is the configured provider’s translation of the actual production query plus a SqlGuid/text ordering witness; no SQL Server service execution or query-performance claim. No full-suite/browser rerun was necessary for this narrowly scoped query correction.
- Native full HTTP import of 100 questions/500 targets improved from 720 binding calls/1,320 submitted statements to 22/24, preserving atomic identity/scope guards. Authoring-page read growth after 1,000 unrelated same-season records was corrected from 3→1,003 to 2→2 using an existing index. Shared bank, assignment and immutable seed budgets remain covered. No new schema migration, real source content or production operation occurred.
- Phase A local implementation/review is complete. Next: explicitly checkpoint/push the source and record remote readback, then implement B1 target review and question selection. Student PBE sessions, independent timed/team rehearsal, deferred disputes, chapter rewards, exporter and external pilot/release gates remain open.

- Phase A pushed checkpoint confirmed: `80dfae5c281cd91fcf952a8c73f1d02b5d4bcfdd` was pushed normally to the existing private `origin/codex/pbe-training` and matched remote readback. It includes A3 source `c1b53b4`/`733e4f5` and the reviewed Phase A gate plus B2/D1 contract refinements. B1 implementation has started from this tip. Production is unchanged.

- B1 design decision: choose the complete bounded question set and repair slot before start. Newly missed targets remain due for the next session; a preselected later variant can repair a miss when two distinct intervening targets have occurred. No dynamic extra card or reroll. This preserves immutable saved sets while supporting targeted replay. The selector will keep separate served and accepted-evidence histories, with transaction ordering for tied timestamps. Implementation/verification are underway.


## PBE replay scheduling work in progress — September 12 UTC

- Initial native B1 persistence regressions pass actual D1 commit, identical/conflicting retry, tied timestamps with accepted sequences, stale concurrent preparation rollback including the caller’s attempt, and view-only service counts. This is an initial native milestone; full native/canonical selector/evidence parity and task review remain open.
- Repair spacing uses a bounded projection of the latest three distinct accepted target IDs and their original accepted sequence. Two other targets after a failed attempt establish spacing; simultaneous parts of that failure do not. This avoids per-card scans of old sessions. Additional multipart/repetition cases and canonical implementation are underway; no student session/UI integration is claimed yet.

- B1 pure scheduling/selection now passes native and C# tests. A behavioral RED in both runtimes showed Simulation selecting only two targets when four were available; counting earlier selections toward target balance corrected it, with distinct due-slot targets retained. Native selector tests cover the requested scope sizes, signed seed fixture, ordinary allocation/repair/transfer and shortened-set mix limits. Adapter projection reads remained 8 rows/4 queries after adding 40,000 unrelated/excluded event/projection records. These are internal synthetic results; canonical persistence expansion and final task verification/review remain underway.

- B1 canonical persistence milestone: eight actual SQLite integration tests pass atomic caller rollback, conflicting retries, concurrent initialization/revisions/service counts, tied multipart chronology and four-query projection reads after 40,000 excluded rows. A selection edge case is being closed: overlapping failed targets must permit a next-session retry instead of making a valid bank unavailable. Such retries will not be described as spaced repair. Final task verification and independent review remain pending.

- B1 local commit `206a3e8` passed full web/native 662 tests/1 optional skip and canonical 114 unit +221 integration/1 optional load skip; final canonical query-extraction check passed 10 focused tests. Independent review has reproduced two selection issues: overlapping rehearsal quotas can shorten a feasible set, and many variants of one neighboring target can keep a failed target out of a short assignment's replay. Findings are being finalized for the original implementer's scoped fix round. B1 remains unapproved and unpushed.


## PBE B1 replay scheduling gate — September 12 UTC

- Native and C# now share deterministic question selection, target-specific review intervals and composable atomic evidence/service persistence. Early successful replays preserve future review dates; due unaided recall advances the interval; failed recall remains due. Recognition and aided success cannot repair failed factual recall. Rubric parts are grouped per target, so partial cards preserve fully correct target results without clearing missed targets.
- Complete sets are selected once for the later session adapter, with distinct coverage/due/repair/transfer roles. Simulation balances targets and respects actual-set commentary/TrueFalse limits; focused Practice/Review can use assigned commentary. Failed targets remain reachable when a small or overlapping bank cannot provide spacing, without claiming spaced retrieval. Viewed prompts and accepted answers have separate idempotent histories; original acceptance time and sequence survive ties and retries.
- Commits `206a3e8` and `6007ab8` passed task review and scoped fix re-review. Review reproduced and closed failed-target starvation behind many same-target variants and false Simulation shortening from overlapping quotas. Shared native/C# regressions verify the exact repeated-session and feasible eleven-card witnesses, plus simultaneous multipart targets. The cited C# compound-line readability issue is also closed. No remaining B1 review findings.
- Verification chronology: full initial web/native 662 passed/1 optional skip; canonical 114 unit +221 integration passed/1 optional load skip. A subsequent unchanged canonical query extraction passed 10 focused integration tests. Final post-review selector/readability fix: native 38 focused, canonical 27 unit +10 integration, both TypeScript checks, lint, scoped C# formatting and whitespace passed. Re-review inspected the fix and retained run logs without rerunning suites. No fresh full-suite claim after the scoped fix.
- Native scoped projection reads remain 8 rows/four queries after 40,000 unrelated/excluded records; actual SQLite uses four scoped commands with equivalent growth. The configured SQL Server translation handles 10,000 IDs through OPENJSON with at most five parameters; no SQL Server service execution or performance claim. Simulation quota feasibility uses four category counts and constant-time candidate checks, avoiding a bank rescan per trial.
- This closes internal B1 logic/persistence, not student-facing delivery. B2 must atomically combine authenticated immutable sessions/cards, frozen grades, server-recorded assistance, fresh source/membership guards and daily effort with these adapters. Complete HTTP costs, saved-session/browser behavior, actual learning retention, pilot and deployment remain open. The plan records exact evidence kinds and caller boundaries; five plans retain thirteen unique tasks, nineteen valid local link targets and balanced code fences. Next: documentation/source checkpoint and push, then B2.

- B1 pushed checkpoint confirmed: `9e7de270d6a789970f6a0f3c6339c4d9d5cbef5f` was pushed normally to the existing private `origin/codex/pbe-training` and matched remote readback; working tree was clean. It includes reviewed B1 source `206a3e8`/`6007ab8`, closed findings and implementation boundaries. Next: B2 daily-study integration. No deployment occurred.

- B2 has confirmed the canonical legacy session relationships and progress writer would couple new introduction sessions to fabricated source/knowledge rows and legacy Honor updates. The approved implementation uses focused PBE session and daily-effort services, sharing existing preference/calendar/day/week storage and atomic guards. Initial HTTP regressions are being added; no daily-study completion gate is claimed yet.

- B2 initial native HTTP RED reproduced requested Pbe silently starting an eight-card legacy Memory session. Initial GREEN passes a two-card frozen PBE set, sanitized prompt, partial multipart grading, refresh, exact/conflicting retries, completion, one target review per target, short-day effort and no legacy mastery writes. Each composed fixture request remains within fifty submitted SQL statements; native typecheck passes. Assistance/revocation/concurrency, large-bank costs, canonical parity and UI/browser gates remain underway. This is an initial milestone, not completed B2.

- B2 native milestone: six HTTP tests pass, including a 5,101-question bank start and eight cards with serve/resume/submit/retry sequences. Canonical HTTP RED reproduced an explicitly assigned introduction-only Pbe request falling into the legacy unassigned-source rejection. Canonical integration is underway. Session eligibility will hash complete sorted eligible source identities/content plus owner/season while enforcing current membership separately and retaining raw concurrency guards, avoiding unrelated introduction assignment revisions invalidating an unchanged personal scope. Final full request counts and security/browser gates remain pending.

- B2 canonical initial HTTP flow now passes an introduction-only two-card session, partial grading, original ID/time retries, contradictory 409, completion/effort and no fabricated legacy SourceUnits. UI integration is using a focused PBE page behind the existing study route to isolate assistance/feedback from the coupled Memory generator UI. Saved server format remains authoritative on resume; canonical discovery, broader security and browser verification remain open.

- Future D1 integration review added the original accepted sequence to the proposed chapter-proof contract. B1 already persists this order; carrying it through prevents equal-time success/failure and later coach corrections from being reordered by incidental arrays or IDs. Required tied-order/correction tests are now explicit in the plan. This is a contract refinement, not completed chapter progress.

- B2 daily/recap milestone: seven native HTTP tests and canonical introduction-only HTTP pass short PBE mission targets/credit and existing recap routes. The saved-format dispatcher and focused PBE page are implemented; 32 existing Memory UI tests plus the pure PBE input regression, native typecheck and lint pass. Known newly created Memory sessions seed the format cache to preserve current input through dispatch. PBE UI/security expansion, mixed-format day/concurrency checks and native/canonical browser journeys remain underway; no blocker or completion claim.

- B2 browser milestone: native Chromium and canonical Chromium/Pixel 7 emulation journeys passed short-bank, eight-verse replay, full-chapter, partial grade/refresh/exact retry, source aid, saved Memory despite a Pbe URL, and 1440/390/320 keyboard/reduced-motion checks. This is browser emulation, not physical-device evidence. Subsequent self-review found a native source-snapshot/guard ordering risk in the optimized composed path; deterministic interleaving regressions and capture ordering are being addressed before final gates. Root also requires proof against a newly inserted-then-revoked assignment contributing unguarded material. B2 remains uncommitted/unreviewed.

- B2 source-race RED reproduced revocation after an old material read incorrectly returning 200. The native resolver now captures scope/assignment values once and filters a single source/pack/revision SQL snapshot from those exact values, instead of rereading live assignments via effectiveSources. B2 composes an exact assignment-row-set proof into acceptance. Thirty-six focused native session/bank/introduction tests pass; composed student 5,101-bank cost improved to 27 statements and maximum authoring import to 16. A transient insertion/removal witness remains underway, so final authorization and task gates are still open.

- B2 transient assignment test passes: sources are selected only from captured assignment values, so insertion during loading followed by deletion before commit adds no card. Actual HTTP precommit revocation rolls back attempt, evidence and effort together; sixteen native cases pass, with one Review-denial assertion/status alignment still being finalized. Unfinished recaps explicitly return 409 before and after invalidation in both adapters; completed historical recaps remain available. Final input/static/browser/task-review gates are pending.

- B2 final focused milestone: canonical PbeStudyTests 13/13 and native/UI focused 55 passed, including due-only Review and Memory→PBE/PBE→Memory day deduplication. Start nonce keys use bounded SHA-256 identities for canonical record-ID limits; stale supplied mission references reject rather than creating a replacement, while saved missions resume normally. Full suites/static checks are running. Final report/commit and independent review remain pending.

- B2 full canonical suite passed 117 unit +235 integration tests/1 optional load skip. First full web run had one existing large-bank five-second timeout while SDK work also ran (688 passed); focused bank/session rerun passed 33 and the bounded-worker full rerun is underway. Root directed remaining heavy gates to run sequentially. Eight-target acceptance plus day credit remains within fifty submitted statements; adding 10,000 excluded rows does not increase start statement count. Final canonical Chromium journey passed; remaining final browser/static results and report/commit are pending.

- B2 final native PBE journey plus legacy eight-card resume and Simulation rejection passed. A broader training browser test reached the expected Memory recap/day/passages but failed on Honors selectors (details/In progress versus requirements/Locked). Root requested baseline `9e7de27` source/fixture or isolated reproduction evidence before accepting a stale-test correction, because these labels can reflect different earning states. Preserve actual legacy Honor-progress assertions; the remaining browser gate and final review are still open.

- B2 baseline comparison resolved the Honors-test concern: at `9e7de27`, HonorsPage already exposed All/Earned/Locked filter buttons and always labeled the mastery dialog “View … requirements”; In progress was a badge, not a filter. The existing training fixture still requested “details” and an In progress button. Root accepted the two selector corrections with dialog/Escape/focus, earned-filter and overflow assertions retained. No Honor product behavior changes; affected browser rerun remains pending.

- B2 final self-review reproduced valid built-in Bible daily access returning 409 after organization scoping was added to a bounded today-read guard. The correction reuses the established builtInContentSql exception from atomic() and adds an actual built-in today/start/next/submit regression. This preserves the existing shared-library permission rule. Final focused verification is underway and will be distinguished from the earlier full-suite results.

- B2 final native browser gate passed all four tests in 20.6s, including the PBE journey and complete legacy Honors/preference/assignment-invalidation flow. A third stale label (“Change weekly goal”) was proven against baseline “Edit weekly goal”; remaining accessible names were checked before rerun and meaningful behavior assertions retained. Native build passed with the existing chunk advisory. Canonical affected browser and final scoped static/format readback are running sequentially; no product commit/review yet.

- B2 final canonical browser gate passed all four desktop/mobile journeys in one minute, including PBE and preserved legacy Honors/preference/assignment-invalidation behavior; final native gate also passed four. Scoped C# formatting is clean. Last lint/typechecks and focused post-self-review tests are running before explicit scoped staging/commit. Report preparation includes RED evidence, baseline stale-selector proof, guard interleavings and exact composed statement counts; independent review remains pending.

- B2 implementation commit `4940d6f` is local and under independent review. Review reproduced one Today navigation defect: publishing a new head against another season source outside the student’s assignment hides the compatible frozen saved mission, while its next/submit still work. The saved personal scope is unchanged. Review is finishing; the original implementer will receive the scoped findings before B2 can be checkpointed or pushed. Full initial suites and final focused/browser chronology are retained in the implementation report; no reviewed gate yet.

- B2 review fix GREEN: 32 native/HQ checks and 14 canonical PbeStudyTests pass. Actual publication-change regressions retain frozen mission identity/count/scope/resume, reject new starts with an empty current bank, grade the original card and remove availability after membership revocation. Final scoped formatting/static checks, commit/report and independent re-review remain pending.


## PBE B2 daily study gate — September 12 UTC

- Native and C# daily study now deliver saved PBE Practice and due-only Review through the existing study/today/recap routes. Full selected sets, question versions, target rubrics and actual short-bank counts are frozen once. Automatic multipart feedback shows points, expected answers and citations only after acceptance; unfinished recaps do not reveal unanswered material. Memory remains explicit and historical saved format wins current defaults/URL. Timed PBE Simulation remains disabled until C1.
- Separate PBE session/mission services combine accepted attempts, target evidence, session state and existing day/week effort atomically without fabricated legacy source/knowledge rows or Honor writes. Response-loss retries preserve exact answer identity, original acceptance time and sequence. Source assistance is saved before reveal and cannot become unaided by refresh or a false hints flag. Mixed Memory/PBE days receive one common day/week credit.
- Fresh current student/season/source/assignment/licensing guards are separate from semantic personal eligibility. Publishing new question heads, assigning another student or disabling only new admission does not invalidate compatible frozen sessions. Real membership/source/assignment revocation blocks continued actions. Native interleaving tests bind source material to captured assignment sets and reject transaction-time revocation without partial attempt/evidence/effort writes; built-in shared Bible content retains its established permission exception.
- Implementation4940d6f and fixc8111c6 passed independent task review and scoped fix re-review. Review reproduced and corrected Today hiding a compatible saved mission when newer question heads moved outside the personal assignment. Native and canonical HTTP regressions preserve the saved resume/grade, reject new empty-bank starts and enforce subsequent membership revocation. No new schema migration.
- Verification chronology: initial full web/native690 passed/1 optional skip and canonical117unit+235integration/1 optional loadskip. The first full web run had a five-second large-bank timeout during concurrent SDK work; focused and bounded-worker full reruns passed with unchanged timeout. Final initial scoped58native/UI,13canonical and15HQ/PBE checks, static/type/lint/build/format checks and browser journeys passed. Native and canonical each passed four final training journeys; a subsequent native PBE entry journey passed. Browser1440/390/320, keyboard, reduced motion and Pixel7 emulation are local emulation evidence. Three stale legacy browser names were proven against9e7de27 and corrected with the actual Honor/preferences/invalidation assertions retained.
- Post-review five-file fix:32native/HQ and14canonical focused tests, both typechecks, lint, scoped format and whitespace passed. No fresh full-suite/build/browser claim after that narrowly scoped Today correction. Existing bundle-size and test color-environment advisories remain recorded; physical devices, SQL Server runtime, actual content approval, human learning outcomes and live capacity are unverified.
- Native complete HTTP budget examples include authentication: start22statements (unchanged after10,000excludedrows), next26, eight-target acceptance plus day credit35; all new session fixture requests assert at most50. A5,101-question scoped student bank uses27statements. These are local SQL invocation/row observations, not deployed Worker CPU/memory capacity. D3 must export all five new session/mission record kinds alongside the prior evidence kinds and scope audit records.
- B2 local reviewed gate; root documentation/source checkpoint and push follow. B3 simplifies long-verse interaction and profiles study-aid evidence while preserving original Advanced Honors. C1–C3 rehearsal/disputes and D1–D3 progress/release remain open. No production change or pilot is claimed.

- B2 pushed checkpoint confirmed: `a84c787a0c23c5bb796f1da74feddf3012d17496` was pushed normally to the existing private `origin/codex/pbe-training` and matched remote readback with a clean working tree. Includes implementation `4940d6f`, reviewed fix `c8111c6` and the documentation gate. B3 starts from this tip; production remains unchanged.

- B3 initial RED/GREEN milestone:39frontend tests cover direct Builder selection and pending retry recovery;17native engine tests cover bounded chunks, varied cues and supported-evidence ceilings. Purpose/permission persistence and canonical integration are underway. Existing canonical RuleProfileSnapshotJson/card PayloadJson can carry additive purpose/generator/profile snapshots without a schema change; native saves equivalent values. Final runtime/browser/Phase B audit and independent review remain pending.

- B3 purpose/permission milestone:14native HTTP/adapter/Honor checks pass enabled Warmup versus eligible Advanced, disabled-season legacy admission, saved resume and grading. Canonical29domain tests plus the actual HTTP Memory-purpose regression pass. UI exposes the purpose choice before creation; optional recitation is explicitly unscored. Saved-snapshot validation, common deterministic fixtures, Phase B synthetic replay/browser/full checks and independent review remain pending.

- B3 all Memory product paths are implemented locally; typed saved-snapshot consistency and purpose/progress/unscored-recitation tests pass. Initial private PBE target-evidence audit reached full8/24scope coverage; rerun is active after correcting a harness assumption about a separate Today Review step. Reviewed B2 has one actual-count Practice step prioritizing due targets, with explicit Review separate, so report the changed workload honestly against the old54-session course. Original21-verse/47-word cue probe shows at most12chunks. Native/canonical browser journeys and final sequential suites remain pending; no reviewed or pushed B3 gate yet.

- B3 real browser RED exposed AuthProvider clearing pending Memory answers on bootstrap /me because previous in-memory identity is null. Root approved a scoped same-sessionStorage owner marker checked against fresh authenticated user/org/kind/role, retaining existing async/cache safeguards and clearing both Memory/PBE attempts on unknown/changed identity, logout or401. Auth regressions and the actual reload journey are required; B3 browser/final gates remain open. No authentication bypass or credential storage is introduced.

- B3 Auth owner prerequisite GREEN preserves Memory/PBE/read-aid entries only for freshly verified same-owner bootstrap and clears both families/marker on unknown/foreign/permission changes,logout/401, with cache/revision guards retained. Full web/native716passed/1optional skip; required canonical gate188passed (95unit+93integration), plus18focused StudyIntegrity tests after the empty historical snapshot self-review correction. Final rebuild and actual native/canonical Memory/PBE/legacy browser journeys are running sequentially. Exact suite/filter chronology will remain in the report; no B3 commit/review gate yet.

- B3 final affected browser gate GREEN: native3/3 in18.7s and canonical6/6 in1.1m across Chromium/Pixel7 emulation. Actual47-word Builder, touch/keyboard/undo/clear, same-owner pending-answer reload after Auth fix, preserved PBE/legacy flows, reduced motion and coach/student1440/390/320 layouts have no measured overflow. Final scoped format/lint/build, audit variant-count report and explicit commit/report are underway. These are local emulated browser results; independent review/push remain pending.

- B3 local implementation commit `66e3255` includes40scoped app/test files; only rootPROGRESS/solo plan remain dirty. Final report read and171kBdiff packaged; /root/b3_review (gpt-6-astra high) is active for independent spec/quality review. Final retained28-day audit uses28sessions/224events,42/42targets and84/84variants;129answers are≥48h after the immediately prior accepted target answer (125different-question subset), with complete private sessions/events/pairs and independent recomputation. Earlier136/142aggregates are historical; no human-retention/D1-stamp claim. Full/scoped/static/browser chronology remains in the report; B3 unapproved/unpushed, C1 not dispatched.

- B3 review investigation: saved-purpose validation covers session snapshots, but the reviewer is checking whether missing/contradictory new card profile/version metadata can fall through actual mastery/Honor adapters to legacy/Advanced evidence. No final finding/severity yet; task remains unapproved while exact reachability and focused evidence are evaluated.

- B3 review confirmed one saved-card integrity gap: actual grading can treat a new Warmup card with missing/unknown/contradictory profile as legacy/Advanced (focused69→87 witness versus required70). This concerns malformed persisted metadata, not a demonstrated client-forgery path. The original implementer is receiving a scoped acceptance/Honor validation fix with native/canonical HTTP regressions. Review otherwise accepted task behavior and independently reproduced retained audit counts; B3 remains unapproved/unpushed.

- B3 snapshot fix local commit `1a5a473` rejects inconsistent new card/session profiles before new evidence writes while valid Warmup/Advanced and unversioned legacy controls still submit. Native33focused tests/typecheck, canonical37unit and19StudyIntegrity passed. Remaining lint/scoped formatting readback and independent fix re-review precede the Phase B checkpoint; no new broad suite/browser/audit claim for this backend-only fix.

## PBE B3 Memory and Phase B local gate — September 12 UTC

- New enabled-season Memory sessions offer varied Warmup or an explicitly coach-permitted Advanced challenge. Purpose, generator and evidence profile are saved with sessions/cards; historical missing fields retain original generation and evaluation on resume. Warmups cap wording evidence at 70 or the lower existing ceiling, without reducing an already-higher score. Builder remains sequence evidence, optional recitation is unscored, and permanent Honors retain their requirements.
- Direct Builder selection preserves duplicate token identities and restores uncertain submitted text without reinterpreting it. The tested 47-word verse needs 12 correct chunk selections plus submit. All 21 sample verses produced more than one Warmup mask across 20 seeds; not every replay has a unique mask. Student and coach progress explain existing prerequisites independently of authored PBE requirements.
- Real reload testing exposed bootstrap cleanup deleting pending answers. Auth now compares a non-secret sessionStorage owner marker with freshly authenticated user/org/kind/role before preserving Memory/PBE pending data and read-aid markers. Unknown or changed identity, 401 and logout clear them. Existing cache/revision guards and server authorization remain. Self-review also corrected final-token focus and retry of an empty historical canonical snapshot.
- Implementation `66e3255` and snapshot fix `1a5a473` passed independent task review and scoped fix re-review. Review reproduced malformed Warmup card metadata falling back to larger legacy evidence. Both actual submission paths now validate saved session/card version and profile consistency before new attempt, mastery, proof or Honor writes. Valid Warmup/Advanced and unversioned legacy controls still submit; already accepted historical attempts are not reclassified.
- Verification chronology: full web/native 716 passed / 1 optional skip, followed by 17 expanded Auth checks. Required canonical study/training/PBE/activity/mastery/legacy checks passed 188 (95 unit + 93 integration), followed by 18 StudyIntegrity checks after the historical snapshot correction. Both builds, typechecks, lint, scoped format and whitespace passed. Native 3 and canonical 6 browser journeys covered real pending-answer reload, preserved PBE/legacy study, keyboard/touch, reduced motion and coach/student 1440/390/320 layouts without measured overflow. After review, the backend-only snapshot fix passed 33 native checks, 37 canonical unit checks, 19 StudyIntegrity checks and scoped static checks. No broad suite/browser/audit rerun is claimed after that fix.
- Synthetic native replay covered all 16 targets / 32 variants in each 8-verse, 128-exercise case; all 48 targets / 96 variants in the 24-verse, 96-exercise case; and all 42 targets / 84 variants in 28 daily sessions / 224 exercises. Final retained 28-day evidence contains 129 correct target answers at least 48 hours after that target's immediately previous accepted answer, including 125 different-question pairs. Raw synthetic IDs, times, accepted sequences and independent recomputation remain ignored; the reviewer verified the retained hashes and counts. Earlier 136/142 aggregate runs are historical. Supplied correct answers establish coverage and bookkeeping, not human retention or D1 chapter certification. The new Today course uses one Practice step prioritizing due targets, so workload differs from the earlier split Review/Practice course.
- This is a reviewed local Phase B gate; source checkpoint/push follows. C1–C3 timing, team rehearsal and disputes plus D1–D3 chapter progress and release validation remain open. D3 must preserve canonical RuleProfileSnapshotJson/card PayloadJson and native purpose/generator/profile/memoryChallengeRequest fields. Existing build-size and test color-environment advisories remain deferred. No physical-device, SQL Server runtime, production capacity, deployment or learner-pilot claim is made.
