# Embroidered wordmark and mobile Coach header — September 11, 2026

The user approved local background removal for the already-approved stitched Erudoza name. During the resulting preview, the user identified the cramped mobile Coach bar with a clipped Seasons shortcut and oversized All sections button. This follow-up completes lettering integration and addresses that mobile navigation feedback in the isolated `codex/honors-public` worktree.

## Lettering and source preservation

- The original approved 2172 × 724 RGB PNG remains byte-identical. A saved [preparation script](../brand/2026-09-11-patch-wordmark/prepare-alpha.mjs) checks its SHA-256, removes connected paper regions, preserves the pale thread interior, and resolves the antialiased edge against nearby opaque thread. This local method was explicitly authorized after failed built-in transparency output.
- The [transparent master and manifest](../brand/2026-09-11-patch-wordmark/README.md) preserve the original canvas. Production WebPs use a 2114 × 518 crop with eight source pixels of safe alpha padding, downscaled to 320 × 78 and 640 × 157. They are 17,594 and 51,138 bytes respectively. All three files have genuine alpha; every one of the master’s 656,449 opaque pixels retains original RGB, including pale highlights. Independent asset/source review verified all hashes/dimensions and found no actionable defect.
- `ErudozaWordmark` renders the name through shared `PatchArtwork`. Literal screen-reader text preserves the brand name without duplicate image announcements. The existing compact logo, account/public tagline rules and responsive header allocation remain. Lettering receives the same pointer lift/dip and contour shadow as the logo/Honors.
- Navy/ivory/white contact sheets and rendered desktop/phone headers were inspected. Browser widths ranged from 115 to 160 pixels for the name; complete letters remained visible and legible without a rectangular matte.

## Initial wordmark gate

- Frontend Vitest: **314 passed across 41 files**. Full ESLint, web/native TypeScript and native/canonical builds passed.
- Existing local browser recipes were reused under the pstack-verification workflow. **33 views** at 1440/390/320 passed for student HQ/Study/Honors/Progress/recap, Coach, public landing, and account pages. Checks covered actual name image dimensions/load/accessible text and background/banner invariants, with no overflow, broken images or JavaScript errors.
- **19 motion checks** passed, including the new lettering across seven header surfaces, shared reduced motion/exit reset, stationary pointer bounds, and coarse-touch rest on the shared logo primitive. Existing goal/Honor focus restoration and active shortcut recovery remained green. Main report: ignored `.local/wordmark-integration-browser/report.json`, completed at **2026-09-11T19:08:30.008Z**.
- Enabled signup/recovery email and code-entry screens plus invitation missing-token state passed **15 additional views** at 1440/390/320. The 300px local security-widget substitute fits the 304px form at 320px. Two code receipts were intercepted only in the test browser. No real CAPTCHA/email/account/password completion occurred. Evidence: ignored `.local/wordmark-account-forms/report.json`.
- These wordmark gates precede the subsequent mobile Coach header refinement below. The old preview process had expired between turns; a read-only readiness check returned connection refused and no matching Node process existed. The owned loopback fixture was relaunched at 8885, with fresh synthetic data. No unrelated server was stopped.

## Final mobile Coach gate

- Coach phones now show two complete shortcuts in the saved order, including the current section, beside a compact 44px All sections icon. Search is also 44px. Inline pins move into the command center on phones; desktop pins and the stored order remain intact. Long labels wrap within their buttons. Only the redundant home breadcrumb is hidden; nested section switching remains available. Student defaults retain the approved Training HQ/Study/Honors strip.
- Six regression cases cover default/reordered pins, current destinations beyond the first two pins, an unpinned current destination, empty saved pins, and unpinning followed by onward navigation. Final frontend Vitest: **320 passed across 41 files**. Full ESLint, web/native TypeScript and both production builds passed after the Coach changes. Independent source/accessibility review found no actionable issue.
- The final native preview served the matching `index-CMcnNv7y.js` and `index-DIh3HZUO.css` assets. Native JavaScript SHA-256: `2c24917957580cdf47198a16363d5cf2f12e92b5cddfd1cdf89dd527dac40b1c`. Native/canonical main JavaScript sizes were 506.33/506.76kB (149.96/150.10kB gzip); shared CSS was 125.85kB (22.69kB gzip).
- **27 final browser views** passed at 320/390/1440: seven Coach destinations plus student HQ/Honors. Checks confirmed complete labels and shortcut bounds, 44px phone targets, hidden inline pins on phones, full desktop pins, visible current sections, and no horizontal overflow or JavaScript errors. Inspected the actual 320px Overview and long-label screenshots.
- **Seven navigation flows** passed: the formerly clipped Seasons link, All sections dialog/focus restoration, navigation to a hidden section, current-section visibility after unpin/reload, the two longest saved labels together, nested season switching, and student mobile pin management. Evidence: ignored `.local/mobile-coach-header/report.json`, completed **2026-09-11T19:21:25.976Z**. The script's expected long-label order was aligned with the deliberately preserved saved order before execution; there was no application correction after the final browser run.
- The enabled-account and 33-view wordmark runs above were not repeated after this Coach-scoped adjustment; student/shared navigation was checked in the final 27-view run. The owned preview was refreshed with the final native build and fresh isolated fixtures, not hosted data.

## Scope limits

### Later text-name correction

After reviewing the integrated image, the user requested restoring Erudoza as text. The shared component now renders its original text name; the embroidered flame-and-Bible emblem and its motion remain. The image-only lettering stylesheet was removed. Masters/derivatives are archived unchanged, and the design guidance/gallery record the superseding decision.

Post-correction checks: **24 focused tests** passed (wordmark and navigation), ESLint, both TypeScript configurations and both production builds passed. **12 browser views** across landing, sign-in, Coach and student at 320/390/1440 confirmed visible text, loaded patch emblem, absent lettering images, and no overflow/JavaScript errors; actual Coach 320 screenshot inspected. Report: ignored `.local/text-wordmark-check/report.json`, completed **2026-09-11T19:33:07.248Z**. Native JavaScript SHA-256: `61e029e01985a6923b5a563c93df3c2f013c11dee2e1cf8949639236dec0abf7`. Earlier 320-test/27-view results remain historical; the complete suites were not repeated for this restoration.

This is local presentation/interaction evidence. No production deployment, merge, hosted data/email/account action, database migration, full backend/.NET/gameplay rerun, or regional load test is implied. The native/canonical builds retain the existing nonblocking 500kB bundle-size and canonical `punycode` dependency advisories. Source checkpoint and push status are recorded in `PROGRESS.md`.
